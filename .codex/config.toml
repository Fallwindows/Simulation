# Project defaults; validated with the installed Codex, not a global replacement.
model = "gpt-6-luna"
model_reasoning_effort = "extra-high"

[agents]
enabled = true
max_concurrent_threads_per_session = 6
default_subagent_model = "gpt-6-luna"
default_subagent_reasoning_effort = "extra-high"
