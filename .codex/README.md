# Optional Codex project configuration

`config.toml` contains lightweight project defaults. Files in `agents/` are
optional role templates; they do not require delegation or independent review.
The current `/goal` and `EXECUTION_OVERRIDES.md` define authority. Legacy video,
storyboard, model, and review procedures apply only when the current goal
explicitly invokes them. Runtime model differences never require stopping.