#usda 1.0
(
    customLayerData = {
        string creator = "URDF USD Converter v0.3.2"
    }
    endTimeCode = 1000000
    kilogramsPerUnit = 1
    metersPerUnit = 1
    startTimeCode = 0
    upAxis = "Z"
)

def Scope "Materials"
{
    def Material "asimov_orca_adapter_metal"
    {
        color3f inputs:diffuseColor = (0.013411746, 0.021980947, 0.033104762)
        float inputs:metallic = 0
        float inputs:opacity = 1
        float inputs:roughness = 0.5
        token inputs:wrapMode = "repeat"
        token outputs:displacement (
            displayGroup = "Outputs"
        )
        prepend token outputs:displacement.connect = </Materials/asimov_orca_adapter_metal/PreviewSurface.outputs:displacement>
        token outputs:surface (
            displayGroup = "Outputs"
        )
        prepend token outputs:surface.connect = </Materials/asimov_orca_adapter_metal/PreviewSurface.outputs:surface>
        token outputs:volume (
            displayGroup = "Outputs"
        )

        def Shader "PreviewSurface" (
            apiSchemas = ["NodeDefAPI"]
        )
        {
            token info:id = "UsdPreviewSurface"
            prepend color3f inputs:diffuseColor.connect = </Materials/asimov_orca_adapter_metal.inputs:diffuseColor>
            prepend float inputs:metallic.connect = </Materials/asimov_orca_adapter_metal.inputs:metallic>
            prepend float inputs:opacity.connect = </Materials/asimov_orca_adapter_metal.inputs:opacity>
            prepend float inputs:roughness.connect = </Materials/asimov_orca_adapter_metal.inputs:roughness>
            token outputs:displacement
            token outputs:surface
        }
    }

    def Material "white"
    {
        color3f inputs:diffuseColor = (0, 0, 0)
        float inputs:metallic = 0
        float inputs:opacity = 0
        float inputs:roughness = 0.5
        token inputs:wrapMode = "repeat"
        token outputs:displacement (
            displayGroup = "Outputs"
        )
        prepend token outputs:displacement.connect = </Materials/white/PreviewSurface.outputs:displacement>
        token outputs:surface (
            displayGroup = "Outputs"
        )
        prepend token outputs:surface.connect = </Materials/white/PreviewSurface.outputs:surface>
        token outputs:volume (
            displayGroup = "Outputs"
        )

        def Shader "PreviewSurface" (
            apiSchemas = ["NodeDefAPI"]
        )
        {
            token info:id = "UsdPreviewSurface"
            prepend color3f inputs:diffuseColor.connect = </Materials/white.inputs:diffuseColor>
            prepend float inputs:metallic.connect = </Materials/white.inputs:metallic>
            prepend float inputs:opacity.connect = </Materials/white.inputs:opacity>
            prepend float inputs:roughness.connect = </Materials/white.inputs:roughness>
            token outputs:displacement
            token outputs:surface
        }
    }
}

