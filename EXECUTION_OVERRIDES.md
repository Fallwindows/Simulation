# Current execution overrides

These owner-authorized rules supersede conflicting model, effort, delivery,
startup, and execution instructions elsewhere in this repository. Preserve all
unrelated requirements, especially technical correctness, visual fidelity,
provenance, exact-candidate review, and independent acceptance.

## Routing and responsibility

- Use `gpt-5.6-sol` with `high` reasoning effort for the main orchestrator and
  every subagent: programmers, explorers, technical specialists, code reviewers,
  and visual reviewers. Do not use Astra, Luna, medium effort, automatic model
  escalation, or a premium/Fast service tier.
- Pass Sol/high explicitly on every spawn. A role name or filename does not select
  a model. On the first real task, check exposed worker routing. Stop and report a
  concretely misrouted worker instead of continuing.
- Preserve separate assignments for orchestration, implementation, technical
  review, and visual review. An agent cannot accept its own implementation.
- Programmers own self-testing, coherent candidate commits, responses to reviewer
  findings, resubmission, and integration support. Review exact coherent
  candidates and required combined states, not private save points.
- Respect the observed session limit. Start with at most two implementation
  workers and one dedicated reviewer where capacity permits. Reserve review
  capacity, serialize heavy GPU jobs, suspend idle agents, and avoid polling.

## Delivery

- Deliver native 1920×1080, 30 fps, exactly 45 seconds and 1,350 contiguous
  frames, preserving all 12 approved storyboard shots.
- Use 1280×720 for inexpensive previews. Defer 4K entirely for this run.
- Retain visual-quality and technical-correctness requirements. Delivery
  resolution does not automatically reduce sensor-input resolution or alter
  calibration.

## Reuse and implementation

- Reuse `IMPLEMENTATION_JOURNAL.md` and `review/setup-20260920/report.md`. Do not
  repeat environment discovery, reference verification, routing rehearsals, or
  artificial faulty-commit tests unless changed evidence requires it.
- Treat the documented recorder/`libcblas.dll` failure as the first implementation
  repair. Pursue the smallest policy-compliant remedy. Do not disable security,
  bypass policy, or reinstall the whole stack. Reuse the completed installed
  package repair unless new evidence invalidates it.
- Continue independent asset, scene, and presentation work while blocked. Never
  label a blocked recording or dependent test as passed.
- Preserve the documented worktree bootstrap for uncommitted canonical
  instructions, configuration, roles, and references. Copy only absent files,
  compare hashes for existing files, and stop on mismatch.
- Prioritize an improved representative render and a small end-to-end technical
  sequence before broad scene expansion. Use 720p previews and targeted frames;
  reserve full-film validation for relevant integrated milestones.

## Budget and evidence

- At the first production `/goal`, record a roughly 12-hour wall-clock start and
  deadline and carry it across resumed sessions. Reserve time for rendering,
  validation, and evidence publication. The deadline does not waive correctness.
- Publish coherent checkpoints within existing authorization, using
  `review/README.md` as the entry point. Record exact source/input versions,
  actual frames/clips, tests, reviews, and unresolved goals. Do not force-push,
  expose secrets, publish unauthorized assets, or make unapproved purchases.
- Use the owner's chat for consequential decisions and stalled diagnosis through
  focused evidence packages. Routine implementation and reviewer-directed
  revisions proceed without additional owner approval.
- At the budget boundary, checkpoint safely and report the strongest actual
  result and unfinished goals honestly.

This configuration-only update does not start production. Wait for `/goal`.
