# Simulation execution guidance

Read `SIMULATION_VIDEO_IMPLEMENTATION_SPEC.md` (especially startup sections 0–4,
15–22), `agent_roles/README.md`, and your assigned role in `agent_roles/` before
work. Original visual targets and checksums are in `references/manifest.json`.
Keep `IMPLEMENTATION_JOURNAL.md` authoritative; preserve existing user work.

`EXECUTION_OVERRIDES.md` is the current owner-authorized routing, delivery, and
budget authority where older project documents conflict. Use GPT-5.6 Sol with
high reasoning effort for the orchestrator and every subagent, with explicit
model/effort spawn parameters. Keep implementation and review in separate agent
assignments; route every rejection to the responsible programmer and require a
new review for changed candidates or integration bases. Observe actual runtime
permissions and concurrency limits; configured defaults do not override live
session limits.

Do not repeat completed setup discovery or the artificial workflow rehearsal.
Production begins only after the owner's `/goal` instruction.
