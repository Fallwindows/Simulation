# Implementation Journal

## Phase 0 — live rediscovery

**Date:** 2026-09-12
**Status:** Isaac/ROS runtime and native RTAB-Map build verified; implementation is ready for the consolidated runtime test session.

Observed live state:

- Windows 11 Home v25H2, build 26200 (confirmed by Isaac's checker and runtime).
- NVIDIA GeForce RTX 4070 Ti, driver `595.71`, 11.99 GB VRAM; an AMD integrated adapter is also present.
- C: free space before installation: 841.6 GiB; Isaac archive MD5 matched NVIDIA's published `a07968e980072c9ca27b2166443e2d89`.
- Repository is `Fallwindows/Simulation`, branch `master`, remote `origin` points to the expected GitHub SSH URL.
- Port 8080 is free.
- Isaac Sim 6.1.0 installed at `C:\isaacsim`; compatibility checker result: **PASSED**.
- Pixi 0.80.0 and the current native Windows ROS 2 Jazzy workspace installed at `C:\IsaacSim-ros_workspaces\jazzy_ws`.
- `rclpy`, `sensor_msgs_py`, FastAPI, Uvicorn, and OpenCV are available in the Pixi environment.
- `rtabmap_ros` and `rtabmap` source are checked out in the external Pixi workspace and built with Visual Studio Build Tools 2022, MSVC v143, CMake/Ninja, PCL, and the Windows 11 SDK.

Security cleanup:

- Removed the previous handoff document's private-key path, fingerprint, and credential details.
- Repository documents now refer to the existing local Git configuration without exposing credentials.
- Runtime logs, bags, maps, caches, and secrets remain ignored.

## Frozen contracts

- Frames: truth visualization `sim_world -> truth_sensor_rig`; estimator tree `map -> odom -> sensor_rig -> camera_link -> camera_optical_frame` and `sensor_rig -> lidar_link`.
- SLAM owns `map -> odom`; the simulator never publishes that relationship.
- Ground truth is `geometry_msgs/msg/PoseStamped` on `/sim/ground_truth/pose` in `sim_world`.
- RGB: `/sim/camera/rgb/image_raw` and `/sim/camera/rgb/camera_info`.
- LiDAR: `/sim/lidar/points`.
- Clock: `/clock`.
- Estimated odometry: `/slam/odom`; map cloud: `/slam/map_cloud`.
- Dashboard: `127.0.0.1:8080`.
- Internal units: SI; aisle axes are x-forward, y-width, z-up.

## Implemented milestones

- Contracts and JSON-compatible YAML configuration loader with typed dataclasses and early validation.
- Deterministic procedural aisle geometry: floor, two shelf rows, bays, levels, uprights, and seeded product proxies.
- Straight and walking trajectories with deterministic bob, sway, yaw, pitch, and speed variation.
- Quaternion and rigid-transform helpers, including camera optical-frame conversion.
- Optional Isaac USD geometry adapter with delayed imports plus the real Isaac 6.1 runner in `simulator/runtime/isaac_sim_runner.py`.
- Dashboard state/cache, FastAPI endpoints, MJPEG route, LiDAR/map WebSocket routes, and minimal browser page.
- ROS 2 topic contract and RTAB-Map LiDAR launch boundary with simulator truth kept separate from odometry.
- ATE/RPE/distance metrics and run artifact serialization.
- Windows-oriented scripts for native Isaac runtime, Zenoh router, dashboard, mapping, and baseline orchestration.
- Walking USD runtime applies the complete sampled trajectory orientation; `run_sim.ps1` supports GUI by default and explicit `-Headless` mode; Pixi/workspace paths are resolved from PATH/environment or explicit parameters.
- Browser LiDAR and accumulated-map viewers consume `/ws/lidar` and `/ws/map` through separate WebSocket/viewer instances.
- Layered testing guide and implementation handoff.

## Checks

- `python -m unittest discover -s tests -v` — passing after the runtime, launcher, and dashboard additions.
- deterministic preflight via `python -m simulator.runtime.sim_runner` — baseline, walking, and sensor-realism scenarios pass.
- `python -m compileall -q simulator dashboard evaluation tests ros2_ws/src/grocery_sim_mapping` — passing.
- staged `git diff --check` — passing before commit.
- Isaac compatibility checker — passed.
- Isaac headless runtime smoke — passed; actual aisle USD, camera ROS graph, native OmniLidar, clock graph, TF publisher, and ground-truth publisher executed.
- Cross-process ROS via Zenoh — verified: camera image, camera info, LiDAR cloud, ground truth, `/clock`, and TF were discovered and received by Pixi ROS CLI processes.
- Dashboard live integration — verified: `/api/health` passed and `/api/status` reported RGB and LiDAR connected with fresh samples and 29,498 LiDAR points.
- Visual Studio Build Tools verification — Build Tools 17.14.40, MSVC `14.44.35207`, CMake tools, and Windows SDK `10.0.22621.0` present.
- RTAB-Map native build — `rtabmap`, `rtabmap_msgs`, `rtabmap_conversions`, `rtabmap_sync`, `rtabmap_util`, `rtabmap_odom`, and `rtabmap_slam` built successfully with Ninja/MSVC; `rtabmap_odom` executables and `rtabmap_slam/rtabmap.exe` verified in the install tree and as live ROS processes.
- ROS package verification — `ros2 pkg prefix rtabmap_odom` and `ros2 pkg prefix rtabmap_slam` resolve successfully; `ros2 pkg executables` lists `icp_odometry.exe`, `rgbd_odometry.exe`, `stereo_odometry.exe`, and `rtabmap.exe`.
- RTAB-Map live launch — initial Windows parameter typing caused `rtabmap` to exit with `3221226505`; changing slash-qualified RTAB-Map internal parameters to strings and using the native LiDAR-only 3D path fixed startup. A real Isaac run produced `/slam/odom` processing and nonzero `/slam/map_cloud` data accepted by the consolidated launcher.

## Version-sensitive runtime assumptions

The runner follows the installed Isaac 6.1 examples: camera helpers and camera info use the current ROS bridge graph, LiDAR uses a schema-created native `OmniLidar` with the current tick-rate API and `RtxLidarROS2PublishPointCloud`, and `/clock` uses `ROS2PublishClock`. TF is published as a ROS `TFMessage` with the frozen contract frame IDs because Isaac's computed-tree helper emits `world` for the stage root.

## Known risks

1. RTAB-Map optional integrations such as `grid_map`, AprilTag, and ArUco were not required for the LiDAR ICP/SLAM baseline.
2. The dashboard uses raw ROS Image decoding with OpenCV and does not require `cv_bridge` at runtime.
3. The browser page uses a pinned Three.js module for point display; ROS/backend integration is verified, while browser rendering and map quality remain visual/runtime QA steps.

## Git synchronization

Earlier implementation milestones and handoff cleanup were pushed to `origin/master`. The current native Isaac/ROS runtime milestone is the verified working-tree update described above and is pushed after the final test pass; the exact SHA is recorded by the final handoff message.

## Final correctness and integration pass

**Date:** 2026-09-12

- Separated truth TF from estimator TF: the simulator publishes `sim_world -> truth_sensor_rig`; RTAB-Map owns `map -> odom -> sensor_rig` and static sensor children.
- Made the mapping baseline explicitly LiDAR-only and passed scenario-selected mapping parameters into the packaged launch. Root and packaged contracts/configs are equality-tested.
- Corrected the USD camera basis to the ROS-compatible optical convention and added basis regression coverage. The walking runtime applies the complete sampled quaternion to the actual USD `SensorRig`.
- Added initial SE(3) evaluation alignment, orientation RMSE, quaternion CSV fields, bounded tracking freshness states, one-second live metric throttling, and Isaac clock/sensor timestamp-offset status.
- Added run-isolated status/artifact validation, scenario-derived collector duration, realtime-by-default launch behavior with `-Fast`, explicit `run_sim.ps1 -Gui/-Headless`, and RMW/ROS-domain defaults for the dashboard.
- Added separate orbit/autofit map/LiDAR viewers using independent `/ws/map` and `/ws/lidar` streams.
- Final live baseline `runs/20260912-155756059/`: 1,230 Isaac frames, RGB/LiDAR/clock/ground-truth samples, native RTAB-Map database, nonzero map stream, quaternion CSVs, `initial_se3` metrics, and no runtime errors in the preserved logs beyond the Windows ROS log-symlink warning.
- Live dashboard probe during `runs/20260912-142156510/`: `/api/health` passed; RGB, LiDAR, and map caches were connected with 23,924 LiDAR points and 3,561 accumulated map points; `/api/metrics` reported `tracking_state=tracking` and `initial_se3` alignment.
- Live walking smoke `runs/final_walking_runtime_smoke.json`: 120 frames completed and the final rig quaternion was non-identity, confirming full sampled orientation reached USD runtime state. Live sensor-realism smoke `runs/final_sensor_realism_runtime_smoke.json`: 21 noisy clouds received and published with timestamp offsets recorded. The GUI smoke `runs/final_gui_runtime_smoke.json` completed 120 frames without `--no-window`.
- The collector now waits for the first live sample before measuring its scenario-derived capture window, preventing Isaac startup time from consuming the evaluation interval.
- Final automated suite: 39 tests passing; Python compileall and PowerShell parse checks passing; native RTAB package prefixes and executables verified.

## 2026-09-20 — specification startup preparation (G00)

Owner scope: setup only; no full production run. Main checkout preserved at
master/d5e825c8f6dab77aa6a1007c9731c226b588dfcf. Supplied untracked specification
and handoff remain intact. Added short AGENTS.md pointer, copied verified roles
and all 12 hash-matching storyboard images to expected root paths, configured
project Astra/medium, Luna/high and dedicated Sol/high using installed Codex
0.155.0-alpha.9.2. Actual host turn-context routing confirms those settings.
All six disposable rejection/revision/approval/stale/base-move/missing-evidence
rehearsal cases completed with independent Sol verdicts. Main source unchanged.

G00: workflow verified; technical baseline partially verified; saved visual
baseline BLOCKED. A 180-frame Isaac smoke observed 88 RGB callbacks/29 LiDAR
clouds. Windows Code Integrity blocks the recorder dependency libcblas.dll in
the ROS Pixi environment; policy owner approval or an approved compatible build
is required before the recorder smoke can yield an inspectable frame. No bypass.
Teardown InvalidHandle traceback remains recorded, not certified as clean exit.

Scoped setup repair: backed up installed grocery_sim_mapping surfaces and rebuilt
only that pure-Python package with Pixi --as-is. Installed launch/config hashes,
package prefix/import and launch argument expansion now match/pass. No upgrades.

Start a fresh Codex task/session to pick up project defaults; live limits still
need checking (this session: four total slots, full-access override). Existing
uncommitted setup files require verified bootstrap into new worktrees. G01-G09
not started; no pushes or production source commits. Full evidence and exact
remaining blocker: [setup report](review/setup-20260920/report.md).

## 2026-09-23 — forensic repair pass (G01)

Owner-authorized implementation goal started at 2026-09-23 05:18:49 UTC with a 12-hour checkpoint deadline of 2026-09-23 17:18:49 UTC. Repair work is isolated from the user's dirty main checkout; base is d5e825c8f6dab77aa6a1007c9731c226b588dfcf. The review is pinned to ab8ebbd51989342c3b1acb6b8b96cf947288cd55; every finding must be rechecked against the repair base. Working ledger: review/FORENSIC_REPAIR_PASS.md. No code candidate is accepted yet; independent Sol review is required. Local commits only; no push or merge authorized by this entry.

## 2026-09-23 — forensic repair pass completion (G01)

The accepted local source candidate is `2b8fb77aa12ff649bf5fc0eaaccb5efc2007cb25` (tree `ce016082190e387a7b5e4a584db1d839063ea363`, parent `c780edff0cfa3c582da57545f3592703a892a0f9`) on `codex/forensic-repair`. Documentation/evidence commit is pending after independent audit of `review/FORENSIC_REPAIR_PASS.md`, `review/README.md`, this journal appendix, and the scoped evidence directory.

Five independent exact-candidate reviews approved the source tree, including the completed SLAM-path audit and final exact source review with zero blockers. The final review identity is the exact commit/tree above. Historical rejections remain authoritative: `c780edff0cfa3c582da57545f3592703a892a0f9` was rejected for `INT-C780-F01`; `2b8fb77` closes it by validating the consumed pose artifact's role, frame, optimization state, map version, path, size, and SHA-256 and deriving alignment from retained verified bytes.

Recorded checks on the exact candidate: independent full discovery 127/127; evaluator 22/22; reviewer integration selections 81/81 and 71/71; runtime capture 18/18; tracking 31/31; runtime-review span 22/22; runtime-to-tracker and reversed-order probes passed; 69/69 generated retail files were byte-identical; Python compileall passed; PowerShell parsing passed for ten scripts; and `git diff --check` passed. The first orchestrator attempt could not create its worktree-scoped temporary directory, and a second isolated exact-tree copy run under the authorized main workspace produced 46 Windows fixture-`TEMP` permission errors with no assertion failures; an independent exact-tree reviewer completed 127/127 successfully.

Installed Isaac/ROS production replay and restart validation were blocked by Windows Application Control. No 45-second/1,350-frame or 1080p full film, silent master, twelve-shot acceptance package, product-recognition model, evidence-backed full 3D extents, or time-indexed map-history presentation was produced. Visual evidence remains bounded to comparable 1280×720 stills at 0 s and 8 s. No owner acceptance, merge to `master`, push, release, or publication is recorded.

## 2026-09-23 — forensic report and evidence delivery

The independent report/evidence audit passed. Local delivery commit `0310fcf0762fe178d93c3109857c6ed16ad9e263` (tree `a625659c2f650bcb8d87077054ce1eafe0ac2943`) contains the reviewed forensic report, review README, journal snapshot current at that commit, and 11 preview evidence files. This delivery entry supersedes the earlier “documentation/evidence commit is pending” state.

The accepted source commit remains `2b8fb77aa12ff649bf5fc0eaaccb5efc2007cb25`; the delivery commit changed no source code. Nothing was pushed or merged.

## 2026-09-24 — production-quality environment and sensor upgrade (G02)

The owner's new goal is the attached `goal-objective.md` (SHA-256 `66957a1a675181f4a0814ce0684fbc2dc249ccb5e48296401b5d01434e19850c`). Its requirements govern this pass: at least 30 new geometrically distinct store item types, materially more realistic RGB in the actual camera shots, feature-specific and spatially aligned LiDAR visualization, smooth motion through the sequence, technical-overview clarity, and visual inspection of rendered output. Prior implementation plans are investigation context where they do not conflict with this goal.

The isolated integration branch `codex/production-quality-upgrade` starts at `5f9e2f9cc0d0572b66af267e4e6f4669d0095992` (tree `030ef222991a558da5bc72abd118ef38a9290a49`). The modified main checkout is preserved. The existing 1280×720 RGB evidence at 0 s and 8 s shows a populated aisle but repeated package silhouettes, a flat gray backdrop, and sparse store construction; these frames establish the current visual baseline, not acceptance of the new goal. Architecture, runtime feasibility, and visual audits are in progress. No new source candidate or visual acceptance is recorded yet.

Read-only discovery confirmed 34 catalog keys but only about eight physical geometry families and 1,995 placed products. Current RGB frames have an open gray backdrop, unmotivated hard lighting, prototype shelves, repeated package silhouettes, and little store context. The 12-shot presentation code exists on `codex/storyboard-video` at `ab8ebbd51989342c3b1acb6b8b96cf947288cd55`, not on the G02 base. Its full-map technical views and stepped source-pose lookup do not meet the new selective-LiDAR or smooth-motion criteria. A reviewed port must preserve the repaired capture, SLAM, geometry-hash, and tracking contracts on the G02 base.

G02-A is assigned to a separate programmer configured as GPT-5.6 Sol/high on `codex/retail-asset-variety` (base `5f9e2f9`) for new geometry, catalog, and merchandising; asset-manifest input SHA-256 is `431972a8931001380d119e1d5d18bf0d4d74d18f9d8f93a85e747597712d346a`. G02-P1 is assigned to another programmer with the same configured routing on `codex/production-presentation-port` (same base) for the storyboard presentation port and provenance compatibility. The tool does not expose runtime-observed worker model/effort, so that remains unknown. Exact candidate commits and independent review are pending. Scene structure/lighting and selective sensor/motion behavior remain future implementation assignments, not completed claims.

The current host still blocks Pixi OpenCV imports under Windows Application Control. Isaac Sim and FFmpeg are available; Blender is absent. A bounded ad hoc Isaac Replicator attempt did not produce a frame. Representative rendering should attach a lossless RGB writer to the existing proven Isaac runner, then verify actual output before any visual-quality claim. No security-policy change was made.

G02-S is now assigned to a third independently configured Sol/high programmer on `codex/production-scene-lookdev` (base `5f9e2f9`) for the visible store shell, structural PBR, motivated ceiling lighting, and Isaac-side selected-frame PNG capture. Its owned files exclude the active asset and presentation workers' paths. The latest owner instruction favors maximum useful parallelism; only G02-S may launch the heavy Isaac/GPU render while the other workers perform code and asset generation. A separate read-only audit is checking existing film motion and actual LiDAR source data. The dedicated technical reviewer has completed a preparatory checklist but has issued no candidate verdict.

The prior storyboard artifact at `runs/20260921-053814804/presentation-delivery/presentation_final.mp4` is a valid 1920×1080, 30 fps, 1,350-frame baseline from source branch `ab8ebbd`; its SHA-256 is `007b97082233ee537dce897424110ac42b0c4fd9a91bb74e6bec89268c37a2e7`. Independent pixel inspection found its RGB shots continuous, but a one-frame RGB-to-cyan switch at frame 540 and hard technical resets at 660, 750, 840, 960, 1080, and 1200. The technical renderer projects the 451,802-point finalized map throughout; quantized lookup of 17 trajectory poses produces visible jumps in shots 6–7. These observations are a failure baseline for the new selective-LiDAR and smooth-motion criteria, not accepted G02 output.

The same historical capture `20260921-053814804` contains a genuine 0.2 s LiDAR scan of 177,241 XYZ points paired with RGB frame 3 at 0.2 s, alongside configured camera calibration, sensor transforms, SLAM poses, and tracked-object annotations. Existing film receipts do not retain raw scan point indices or camera projections for highlighted objects. G02-L1 is assigned on `codex/production-scan-selection` to create a bounded CPU-only scan/projection/support receipt from the real capture, explicitly labeled diagnostic because that run predates the repaired canonical pose contract. G02-M1 is assigned on `codex/production-motion-smoothing` to implement deterministic eased source motion and reusable pose interpolation. Both are source vertical slices pending candidate commits and independent review; neither alone satisfies selective LiDAR or whole-film motion acceptance.

G02-A revision 1 is submitted for independent exact-source review: commit `36da67b43fc97505171d5f1f457183062eab4eff`, tree `e822d59b790b915570a913c7015b89b470dea30b`, parent `5f9e2f9cc0d0572b66af267e4e6f4669d0095992`. The worker reports 45 new physical assemblies (34 to 79 catalog entries), all instantiated, with the 34 original USDA/texture assets byte-identical, 2,086 placed instances, a 2.74927 m clear corridor, and 17/17 focused tests passing. Full discovery attempts encountered Windows temporary-directory ACL and OpenCV Application Control import errors; no full-suite pass or Isaac visual/performance pass is claimed. Detached review snapshot is `runs/production-quality-20260924/review-asset-36da67b`; technical verdict and integration remain pending.

G02-P1 revision 1 is submitted for independent review: commit `275f868fdd8815cb1d2b1858010a965538bb1c65`, tree `e99b681abd0c537100d2de26fc462380a13e118e`, same parent `5f9e2f9`. The worker ported 23 scoped presentation/provenance/config/fixture paths while leaving repaired capture, SLAM, tracking, and evaluation paths unchanged. Focused presentation and technical-view suites passed 24/24 and 8/8 under Isaac Python. The port adapts canonical `slam_map_poses.csv` and repaired manifest contracts but deliberately retains unfinished dense-map visualization and shot-boundary motion. Detached review snapshot: `runs/production-quality-20260924/review-presentation-275f868`; technical verdict and integration remain pending.

G02-M1 revision 1 is submitted for independent review: commit `4cfc697c0b207d8b2584aaafb168f62e0f403c43`, tree `74a6c24df77a23e9006104bb0ecc762e2ee304ec`, same parent. It changes only trajectory, transform interpolation, and motion tests. The worker reports 15/15 focused and 38/38 related tests passing, with full discovery stopped by two `cv2` import errors in bundled Python. Numeric checks show substantially lower sampled endpoint acceleration/jerk, but the eased traversal shifts mid-run camera x by up to about 1.08 m; the combined visual framing remains unverified. Detached snapshot: `runs/production-quality-20260924/review-motion-4cfc697`; technical verdict pending.

Independent G02-A revision 1 verdict: **REQUEST_CHANGES**, two blockers on exact commit/tree `36da67b`/`e822d59`. `G02-A-F01`: regular shelf merchandise intersects produce crates/fruit in 57 cross-family cases on the same top shelf; the worker must reserve or relocate display space and add a cross-family overlap regression. `G02-A-F02`: regeneration uses an optional Pillow/host-font path and silently changes all 79 PNGs when Pillow is absent; the worker must pin a reproducible font/dependency path or fail closed with explicit prerequisites, then prove exact asset parity in the documented runtime. The 45 new keys, placement count, geometry hashes, original asset byte preservation, and corridor width passed source checks, but technical approval and integration are withheld. Revision 2 is assigned to the same programmer and requires fresh exact-candidate review.

Separate exact-candidate asset visual pre-review also requested changes before hero-shot use. `G02A-V01` finds one repeated Northstar Market square artwork template across unrelated goods and fixtures; `G02A-V02` finds 18 textured-material bindings without UVs, four declared textures never bound, and 16 assets sharing a textured material between UV and non-UV parts. `G02A-V03` finds aspect distortion and bare sides/backs; `G02A-V04` generic material constants; `G02A-V05` primitive banana, broccoli, ice-cream, bread, and wicker constructions; `G02A-V06` register claims of detail not present in authored geometry. `V01` and `V02` are visual blockers. These are source/texture observations, not an Isaac-render verdict. All findings were routed to the same programmer for revision and later independent re-review.

Independent G02-M1 revision 1 verdict: **REQUEST_CHANGES** on `4cfc697`/tree `74a6c24`. `M1-F01`: capture source hashing omits the new top-level trajectory ramp helpers/constants, so a changed motion law could reuse stale capture/map artifacts. The motion worker now owns the narrow `simulator/capture/manifest.py` motion-fingerprint fix and regression. `M1-F02`: extreme but finite quaternion/position inputs can overflow to an invalid zero quaternion or infinite interpolated position; the worker must use stable arithmetic or reject derived nonfinite results. Numeric 600 Hz checks passed for nominal and short runs, but source approval is withheld until a new reviewed commit resolves both. The reviewer measured maximum mid-run linear-x shift as 1.15544 m, larger than the worker's initial estimate; combined camera framing remains unverified.

Independent G02-P1 revision 1 verdict: **REQUEST_CHANGES** on `275f868`/tree `e99b681`. `G02-P1-R1-F01`: the technical renderer hashes raw worktree bytes for its source, plan, and catalog, while the presentation validator uses LF-normalized text hashes. On the actual CRLF Windows checkout, a real renderer receipt is rejected before bundle assembly; synthetic fixtures masked the mismatch. The programmer is revising producer/validator canonical hashing and adding a real renderer-to-validator CRLF regression. Existing repaired capture/SLAM/tracking/evaluation blobs, six-topic validation, canonical pose contract, 12-shot timing, and focused suites passed independent checks, but technical approval and integration are withheld pending a new exact candidate.

G02-L1 revision 1 is submitted for independent review: commit `457f83e3cd3c44b8835bf0e1deb33a76c1423993`, tree `69e9da0fbde0448565efb175e4ab3e535c5b8b58`, parent `5f9e2f9`. Its bounded real-run receipt at `runs/20260921-053814804/diagnostics/scan-selection/frame-000003-track-000429.json` hashes to `04375c462dde07b01c5958c7eb9dccc4b784f5bf18568c0c977f3a8ae7cdfad8`. The worker reports 177,241 raw returns in scan message 24, 49,700 camera-visible returns, 27 within the observed target bbox, three selected front-surface raw indices, 5/5 focused tests passing, and bad-binding rejection. The receipt is explicitly diagnostic legacy evidence with configured—not observed—camera intrinsics; it is not final production LiDAR acceptance. Detached snapshot is `runs/production-quality-20260924/review-scan-457f83e`; technical verdict pending.

G02-M1 revision 2 is submitted for fresh independent review: commit `54497011a9a6fad4ae338e11bb2858b15b4d3da0`, tree `32ea60033eb66c480f45235808e03e42118e6995`, parent rejected revision `4cfc697`. The programmer reports that trajectory dependency hashing now covers module helpers/constants and invalidates downstream map hashes when ramp code changes, while stable quaternion normalization and weighted position interpolation handle or reject extreme finite inputs. Exact candidate capture+motion tests passed 35/35. This is a submitted fix, not approval; fresh verdict and combined visual framing are pending.

Independent G02-M1 revision 2 verdict: **APPROVE**, zero blockers, on exact commit/tree `54497011`/`32ea6003`. The reviewer independently checked helper/constant hash invalidation, comment/docstring stability, extreme finite interpolation, deterministic 600 Hz motion, and 16/16 focused tests. The integration branch fast-forwarded from its unchanged source base `5f9e2f9` to that exact commit; `git cat-file` confirms tree `32ea6003`, and the integrated 16/16 motion tests pass. Only this journal has an uncommitted tracked edit. This accepts the source-motion vertical slice; 1.15544 m maximum camera x redistribution, presentation wiring, rendered framing, transitions, and whole-film visual motion remain open.

G02-P1 revision 2 is submitted for fresh exact review: commit `5aed25760e92407653b0e79d17892b153186f27f`, tree `8f1239511a2703e7f28b4cfb727f432cb3224b23`, parent rejected revision `275f868`. The worker reports one canonical LF-normalized UTF-8 hash rule in the technical renderer and validator, with real receipt generation over explicit CRLF source/plan/catalog files. Exact candidate focused suites passed 25/25 presentation and 8/8 technical tests. Detached snapshot: `runs/production-quality-20260924/review-presentation-5aed257`. Standalone technical verdict is pending; because integration HEAD has moved to `54497011`, a reviewed combined state is also required before integration.

Independent G02-L1 revision 1 verdict: **APPROVE**, zero blockers, on exact commit/tree `457f83e`/`69e9da0`. The reviewer independently rehashed all 14 inputs, parsed the 0.2 s PointCloud2 through separate SQLite/CDR logic, reproduced 177,241 raw/49,700 visible/27 bbox/three front-support points and all selected raw indices, validated the sensor-to-optical transform and frame-3 crop, and passed 5/5 focused tests plus a wrong-binding rejection. Nonblocking `G02-L1-N01`: raw worktree source hashing makes regenerated receipt bytes vary between CRLF and canonical Git checkouts; the published receipt itself matches canonical Git blob content. The diagnostic receipt still uses configured intrinsics, lacks per-return timing, and cannot prove production spatial consistency. Because integration HEAD moved after M1, L1 is technically approved but awaits reviewed combined-state integration.

Independent G02-P1 revision 2 verdict: **APPROVE**, zero blockers, on exact commit/tree `5aed257`/`8f123951`. The reviewer reproduced real technical receipt acceptance after a CRLF round trip for all seven views and verified independent renderer, plan, catalog, and classification mutations are rejected. Focused presentation 25/25 and technical 8/8 tests and compileall passed. This standalone approval does not accept dense finalized-map visualization, stepwise old transitions, or the unreviewed integration combination.

Combined-state trial `codex/g02-combined-trial` merges approved M1 `54497011`, P1 `5aed257`, and L1 `457f83e` from the current M1 integration base, without conflicts. Its exact HEAD is `d066e1770098150ae4575e602ff08eea9f7acb29`, tree `44e64df4f9592dd6e65b342f4ced18942b5fd865`. Independent combined review is in progress before any promotion to integration. Separate P2 selective-LiDAR and P3 transition workers start isolated branches from this trial tree; their work is not yet accepted.

G02-A revision 2 is submitted on `codex/retail-asset-variety`: commit `4e94738dba29a2f6fdf4b92cf374a162fa765605`, tree `0dbc6dfc47280f45e270100bcdf81be43e01285e`, parent R1 `36da67b`. The worker reports zero product/produce cross-family intersections, 2,017 placed instances with all 45 new types represented, category-specific package art, UV label surfaces, and exact generation under pinned Pillow/font/zlib prerequisites. Documented primary runtime tests pass 21/21; Isaac runtime passes 20 with one documented regeneration skip because its zlib is 1.3.1 rather than pinned 1.3.2. The initial revision's technical and visual rejections remain open until fresh independent reviews of this exact candidate; no Isaac beauty or performance acceptance exists.

G02-S representative Isaac RGB evidence at 1280x720 has a valid direct Replicator BasicWriter path, but its first lighting pass was severely underexposed. The brighter second pass at frame 180 remains too dark on shelf faces, with a plain end wall and uniform floor; scene lookdev continues. Three selected-frame capture completed in a single Isaac process on R1, while a separate full ROS-backed test currently fails before capture due to a ROS bridge `rcutils.dll`/`ROS2CameraHelper` runtime issue. A read-only runtime audit is investigating the exact failure and a truthful synchronized capture path. No scene source or visual candidate is approved yet.

Independent combined-state verdict on trial `d066e177`/tree `44e64df4`: **APPROVE**, zero blockers and zero new nonblocking findings. The reviewer verified exact M1/P1/L1 ancestry and byte preservation of each approved candidate's owned paths, no repaired capture/SLAM contract regression, no P1/L1 import collision, and 73/73 focused executable tests in a clean exact-tree snapshot. Existing `G02-L1-N01` remains nonblocking. The isolated integration branch fast-forwarded to this exact reviewed HEAD; journal changes remain uncommitted. This is source integration only; final assets, scene, selective LiDAR, transitions, and rendered acceptance remain open.

Independent G02-A revision 2 technical verdict: **APPROVE**, zero blockers, exact commit/tree `4e94738d`/`0dbc6dfc`. `G02-A-F01` is closed by reserving produce-zone shelf volume and a cross-family intersection regression; `G02-A-F02` is closed by fail-before-write Pillow/zlib/font prerequisites and byte-identical regeneration in the documented runtime. Independent inspection verified all 45 new types placed among 2,017 unique semantic instances, 45 distinct geometry profiles, all 79 USD/PNG hashes, front UV orientation/bindings, catalog stability, support and corridor checks, and preservation of the original 34 assets. Documented runtime focused tests pass 21/21; Isaac runtime focused tests pass with expected dependency skips. Independent source/texture visual re-review and actual Isaac hero-camera/performance gates remain pending.

Read-only ROS capture audit identified a likely runtime-environment defect in the scene wrapper: pre-setting `ROS_DISTRO=jazzy` and placing external Pixi ROS DLLs ahead of Isaac causes Isaac's setup script to disable its bundled ROS path, so unsigned/incompatible Pixi `rcutils.dll` wins. The installed Isaac bundled Jazzy `rcutils.dll` is present and signed; a prior same-install smoke delivered RGB and RTX LiDAR callbacks. The scene worker is restoring the Isaac child process to its bundled ROS while keeping Pixi components in separate processes, then will attempt synchronized short capture. This is an actionable diagnosis, not a completed fix or sensor acceptance.

An isolated asset-plus-current-integration trial merged G02-A R2 without Git conflicts (trial HEAD `adfb3fd`); it has not been promoted. An escalated focused 94-test combined run found two cross-component regressions after 92 successes and one expected skip: the capture invalidation test mutates a USD but leaves the now strictly validated manifest hash stale, and the inventory test still hardcodes 1,995 rather than the new 2,017 placed instances. A sandbox-only attempt had unrelated temporary-directory ACL errors. Both concrete combined findings are routed to the asset programmer for R3 and require new exact review.

Independent G02-A R2 source/texture visual verdict: **REQUEST_CHANGES** on `4e94738d`/`0dbc6dfc` despite technical approval. UV binding (`G02A-V02`) and register truth (`G02A-V06`) are resolved. The nine new category art systems still repeat one house-brand/layout grammar each (`V01`); hero package sides/backs remain plain accent bands (`V03`); scalar-only diffuse/roughness has no microstructure (`V04`); banana/broccoli/bread and wicker hero forms remain obvious overlapping sphere/flat-quad constructions (`V05`). These are source findings before a valid current-candidate Isaac hero render; inherited PNGs in a trial were byte-identical to pre-asset evidence and cannot establish the new geometry's appearance. Revisions are routed to the same asset programmer alongside combined test fixes. Asset integration remains withheld.

G02-P3 transition revision 1 is submitted on an isolated branch from `d066e17`: commit `abb630cd7861e933d6d77ac2650baad67d54ee5d`, tree `ae6d1ba3db7908b73f9b90f2604347adea80ec71`. The worker declares seven symmetric smoothstep windows at historical hard boundaries, exact 1,350-frame budget, source-agnostic FFmpeg xfade, and a frozen historical diagnostic storyboard whose original hash is retained separately from the revised production storyboard. Reported 27/27 presentation tests include a generated 1,350-frame FFmpeg preview and extracted boundary-frame RMS checks; independent exact review is pending. Source clip motion, new LiDAR rendering, and final visual sequence are not accepted by this submission.

Independent G02-P3 R1 verdict: **REQUEST_CHANGES** on `abb630cd`/`ae6d1ba3`, two blockers. `G02-P3-R1-F01`: FFmpeg custom xfade progress descends 1-to-0, but the authored weights treated it as ascending, so all seven purported smooth transitions contain hard jumps at both window endpoints; independent frame 530→531 and 548→549 RMS at the first transition are 160.80 and 158.05. Tests sampled only the center boundary. `G02-P3-R1-F02`: mixed frames lack an editorial/non-cotimed classification, exact held source frame/timestamp bindings, and per-frame weights, so the shot record can imply sensor fusion that did not occur. Both are routed to the P3 programmer for appended R2 and complete-window rendered checks. The historical diagnostic storyboard split, exact 1,350-frame budget, and delivery package mechanics passed the review.

G02-A revision 3 is submitted: commit `ab6c8409462dcc2060ed010a228f22030d17fb51`, tree `aae3f1731a8036898e7f8cce719ceaefcfd81947`, parent R2 `4e94738d`. The worker reports item-specific branding/compositions, UV printed package sides/backs, per-asset normal and roughness maps, continuous/tapered organic forms and volumetric wicker, plus fixes for the two combined capture-test failures. Focused 22/22 asset/geometry and 18/18 capture architecture tests pass in the worker's documented runtimes; Isaac USD opened all 79 assets. Fresh exact technical and visual review and an asset-plus-integration trial test are in progress. R2 approval and rejection do not transfer automatically to R3.

G02-S lookdev r4 at 1280x720 frame 180 is visibly brighter and better grounded than r3: overhead panels emit, shelf faces are readable, floor joints replace dark strips, and stocked end cases add depth. Its PNG SHA-256 is `44299d60a645199bea2a2565a78553619d8a84ad7445bf42576be898b23b16de`; evidence manifest is `978d5e2df675458e6d7e3cbb23eed33da30da7d4fd030c35f955f0312f5e719b` and now binds source/config/asset/renderer identities. It remains pre-M1/pre-G02-assets and CaptureOnly, hence diagnostic lookdev rather than final RGB or sensor acceptance. In normal-mode r4d, the bridge bootstrap was fixed sufficiently to register CameraHelper; the next first failure is remote `Example_Rotary` LiDAR asset-root lookup. The scene worker is authoring equivalent local OmniLidar schema settings and will validate with a Zenoh router and real RTX publisher. No scene candidate is committed/approved yet.

The isolated R3 asset-plus-current-integration trial now has HEAD `615f1f3542aa824d94898fb753006766ed795ffd`, tree `780ca2955470491f2ade72813e9c04f750c8c0af`; its merge was conflict-free. An escalated targeted run of 95 tests across asset, geometry, capture, motion, presentation, technical views, and scan projection passed (two expected dependency skips), closing the two observed R2 combined-test regressions at the executable level. The exact standalone technical and visual R3 reviews, trial combined-state review, and Isaac hero render remain open. The failed sandbox test left inaccessible temporary folders in this *trial* worktree only; tracked Git state is clean.

G02-P2 selective LiDAR first candidate is committed as `f33d909c2d754de7d07597976af32ca62608697b`, tree `32612b14110e868f7f51123bbad028806a204398`, based on `d066e17`. Its source design binds raw PointCloud2, RGB/calibration, scene/trajectory/sensors, perception, and frame IDs to one capture; uses causal past-only scan pairing, age/gap gates, strict field/timing disclosure, bounded caches, post-render hashes, and a fail-closed presentation schema. The historical 15.8 GB capture is explicitly diagnostic and delivery-ineligible; fresh matched G02 capture is required. An independent early architecture audit supplied six blocking contract checks, which the worker reports addressed in this candidate; exact technical review is pending. I visually inspected three untracked historical diagnostic PNGs and found excessive background point density: about 6,686 visible current points in sensor activation, 34,006 map points obscuring a 35-return object ROI, and 52,867 diffuse returns in the final overview. Per-segment visual selection and emphasis need further refinement before the user goal's selective/context-aware LiDAR criterion can be accepted.

G02-P2 visual-selectivity R2 is committed and frozen: `c89700ab60883893410641bf5e013e9fa575af26`, tree `605324404f4ef1fdb459b5d551ab9220948d4cff`, parent R1 `f33d909`. Historical diagnostic midframes now show 2,000 corridor/near-surface current returns, 2,236 sparse object-view context returns with 35 bright raw ROI supports, and 13,725 structurally selected summary returns. The final caption no longer asserts unsupported item locations. The worker reports 13 focused tests and full historical capture binding pass. I inspected these images and find visual selectivity substantially improved, though the physically small ROI remains hard to see without an optional source-preserving crop. Exact independent technical/visual review and fresh G02 capture are pending.

Independent G02-A R3 technical verdict: **REQUEST_CHANGES** on `ab6c840`/`aae3f173`, finding `G02-A-F03`. All 74 bound normal and roughness texture shaders omit the raw color-space declaration; normal maps also omit required `(2,2,2,1)` scale and `(-1,-1,-1,0)` bias before the 8-bit RGB normal feeds UsdPreviewSurface. This can distort lighting on the 37 map-bound assets. The reviewer passed F01/F02, 22/22 asset/geometry, 18/18 capture architecture, 24/24 sampled combined tests, all 79 USDA parse and 256 shader connection paths, but withheld technical approval until the generator, generated USD hashes, and exhaustive shader regression are corrected. The finding is routed to the asset programmer for appended R4. R3 source/texture visual re-review remains in progress; combined Isaac render still absent.

G02-A R5 is the accepted source asset candidate: commit `a4efc0f17f803cd1a249645954679f67d9648b31`, tree `870cee05e75ac433ec4d58791de883ea1e8d8589`, parent R4 `08a6ea8`. R4 closed technical `F03` with raw normal/roughness data textures and normal decode scale/bias, and removed 16 unused map files/claims from eight deliberately untextured assets. R5 closed visual `G02A-R3-V07` by moving the maple syrup title beyond its dark strip; independent full-resolution pixel review confirms the complete label. Independent technical and source/texture visual reviewers both **APPROVED** exact R5 with zero source blockers, while reserving actual Isaac hero-camera acceptance. The combined M1+P1+L1+R5 trial `d48a5fe8938bad48148025812468afa0d52e659c` (tree `dc190d4c2d36f3fa1bf0e6edd05fd04e2c12a7f1`) passed a 95-test focused integration suite with two expected dependency skips. This reviewed source state fast-forwarded to the isolated integration branch; only this journal remains uncommitted.

G02-S scene candidate is committed at `dafd54ffd8d65d4e535a67fa634ed03f69d015c3`, tree `3c5b09905e18c11ec9ac2eebbad96d8bb4f5be24`, base `5f9e2f9`. A normal-mode 180-tick Isaac ROS/RTX run with bundled Jazzy and a healthy Zenoh router exited zero: 88 RGB at 30 Hz, 179 clock messages, 29 real LiDAR clouds of 16,427–20,002 returns, RGB clock maximum offset about 1e-9 seconds and LiDAR offset zero. The status SHA-256 is `bc085119d51a5ba7df77e104b52f1a1fe097617c2ba7231c2f351d5bc18bf206`; console SHA-256 `f9235365b77d3becb8dbed40616eaa90bab9c04c7694d6d9b9315231bb39c106`. Scene-specific 16/16 tests pass. A conflict-free scene+approved-source trial now exists at `e0ceb30d840ca98c30756570fa0caa83223dbcea`, tree `8592a430da75b6e9e82052687e348de94d401de1`; independent exact source review and actual M1+R5 combined 720p hero rendering are in progress. The r4 stills and r4g sensor smoke came from the standalone pre-M1/pre-R5 scene worktree and cannot by themselves establish final goal visual or sensor truth.

Independent G02-P2 R2 verdict: **REQUEST_CHANGES** on `c89700a`/`6053244`, blocker `G02-P2-R2-01`. Although the renderer closes earlier causal pairing, frame/calibration, skew, source-hash, and post-render-hash findings, the technical delivery validator accepted rehashed but internally inconsistent receipts: impossible future scan times, wrong selection mode/context treatment, aggregate counts disagreeing with rows, and missing/future ROI evidence. The P2 programmer owns a stricter schema/cross-field R3 plus adversarial tests. Independent 35/35 focused tests and visual inspection of all seven historical diagnostic PNGs passed within their prior scope; fresh G02 RGB+LiDAR paired capture remains mandatory.

Independent G02-P3 R2 verdict: **REQUEST_CHANGES** on `963685f`/`dd50668`, with the original xfade-direction/endpoints defect closed by independent full-window frame inspection. Residual provenance defect `G02-P3-R1-F02`: technical held-edge frame timestamps were incorrectly copied from a technical data extent despite no validated clip-frame to measurement-time map; a fixture frame labeled 3.967 s was claimed as 20.4 s. The P3 programmer submitted R3 `24eccb358d14e1399c3bdeec896fdb718c465b60`, tree `34f7885036782be7fa8eba3226680d99e29b8902`, separating exact clip PTS and aggregate data extent, with technical measurement timestamps null unless bound. Reported 28/28 presentation tests pass; fresh independent R3 review is pending.

Independent G02-P3 R3 verdict: **APPROVE** on exact commit `24eccb358d14e1399c3bdeec896fdb718c465b60`, tree `34f7885036782be7fa8eba3226680d99e29b8902`. The reviewer inspected moving frames across the entry, interior, and exit of all seven smoothstep windows and confirmed the correct xfade direction, 1,350 contiguous 30 fps output, explicit editorial/noncotimed classification, source clip PTS and held-frame attribution, per-frame weights, and null technical measurement times absent a validated map. The independent combined M1+P1+L1+R5+P3 trial `6a7a355b54bfdac7213c9254fd6d94f9011cd50a`, tree `db72e9e8760649524b13dccb24ddbc7ffa7be07c`, passed exact source/blob and ancestry checks plus 98 focused tests (two documented dependency skips). The isolated integration branch fast-forwarded to this reviewed source state; only this journal remains uncommitted. Final production film and technical imagery are still open.

The scene+M1+R5 combined trial `e0ceb30d840ca98c30756570fa0caa83223dbcea`, tree `8592a430da75b6e9e82052687e348de94d401de1`, rendered actual 1280×720 selected camera frames at approximately 3, 9, and 17 seconds under CaptureOnly. The reviewer reports 46 focused combined tests and 2,017 placed products. My direct inspection finds improved store context and light, but the uniform gray shelving, flat package faces, regular spacing, and colored-rectangle end wall remain visibly stylized. This is diagnostic visual evidence, not photoreal RGB acceptance or a paired sensor capture. Independent scene review and a revision decision are pending.

G02-P2 R3 is submitted at `6e49df678509d475b6c96b8ce9b5bad1b7ae0836`, tree `f3fe0bfc1562df8195201b04eff3492f0642c50a`, parent rejected R2 `c89700a`. The worker reports exact ordered scan/ROI receipt schema, plan-window and selection-mode enforcement, row-derived aggregates, current-scan RGB pair coverage, and ROI method/hash/message/time linkage, with adversarial regressions; its reported 11 complete-presentation tests and nine technical tests pass. Historical diagnostic PNG hashes remain byte-identical to R2. Fresh exact independent review is assigned. Fresh G02 RGB+LiDAR paired capture remains a delivery gate.

Independent G02-S R1 verdict: **OVERALL REQUEST_CHANGES** on exact scene+M1+R5 trial `e0ceb30d`/tree `8592a430`. Source/runtime mechanics passed within the focused scope (46 tests; local RTX LiDAR schema, ROS bootstrap, timestamps, and deterministic BasicWriter), but 1280x720 CaptureOnly frames at approximately 3, 9, and 17 seconds still look stylized. The reviewer specifically found uniform structural color, flat and sparse package faces, repetitive ceiling/shelf/floor rhythm, a placeholder red/yellow/blue rectangle grid dominating the 17-second end wall, and centered tunnel framing with no close readable shelf view. CaptureOnly has no live sensor publishers and is visual evidence only. The scene programmer now owns an R2 revision on a new branch based on the exact combined trial, with the independent findings routed verbatim; fresh candidate and visual review are required.

A separate read-only pixel audit measured shelf median luma only 31–44, with 41–51% of shelf pixels below luma 32 across the representative frames. The highest-value scene revisions are shelf-facing fill without highlight clipping, a physically credible stocked endcase, lighter/refined shelving, denser camera-visible R5 merchandise, and less procedural ceiling/floor treatment. These are visual diagnosis, not R2 evidence. A read-only capture audit also found that `scripts/capture_simulation.ps1` does not forward `-RequireSensorSamples` or inspect Isaac runtime status, and the v1 recorder/archive validator can mark `CAPTURE_COMPLETE` after a clock horizon even when LiDAR or CameraInfo is empty. The scene programmer now owns a fail-closed production capture launcher gate before the final GPU run.

Independent G02-P2 R3 verdict: **REQUEST_CHANGES** on exact `6e49df`/tree `f3fe0bf`; residual `G02-P2-R2-01` remains open. The reviewer rehashed a receipt that paired a t=2.0 s scan to RGB frame 614 and claimed zero skew; validation accepted it despite the bound frame-index time being 20.466666667 s. The positive fixture also self-reports an incompatible frame 3 pairing. Separately, changing the only ROI raw-index digest to all `ff` while leaving the primary union digest unchanged was accepted. Existing 36 focused tests passed, showing a coverage gap. R4 is assigned to the P2 programmer to cross-bind pair/ROI frame indices and timestamps to the hash-verified RGB frame index and verify ROI index membership/digests from immutable source-derived evidence; fresh exact review is required.

Read-only final-film motion preflight found further acceptance risks, not a change to prior P3 source approval. At the 18-second RGB/technical handoff, M1 is still walking at about 1.13889 m/s while P3 clones outgoing RGB frame 539 across the outgoing crossfade half; the film never shows M1's 18–20.5 second stop ramp. In the inherited technical renderer, camera paths can restart/rewind at shots 6–7 and reset at the map/object boundaries, ray counts and highlighted track identity can pop, and the final orbit runs through the last frame without easing. The P2 programmer is checking which issues persist in its selective rewrite; a separate read-only P3 design review is preparing a moving RGB overlap for the first handoff. Final rendered strips and camera/overlay traces will be required. The accepted seven-window compositor alone does not establish whole-film motion quality.

The capture preflight identified a resolution gate: the current scenario sensor is 1280×720, whereas the approved P3 complete-delivery validator rejects any source smaller than 1920×1080. The 720p scene previews are appropriate diagnostics but cannot be silently upscaled into native 1080 delivery. The scene programmer is adding an explicit 1920×1080 production capture scenario with the same 90-degree FOV and observed recalibrated CameraInfo while retaining inexpensive 720p previews; the final source and technical projection must use that production capture.

G02-P2 R4 is frozen for fresh exact review: commit `1b393cc1cfeea45e36a2a0c457a9e56f42d8113b`, tree `360facf85b6f323108f5ab82c11b79b1c0adf0b7`, parent rejected R3 `6e49df`. The programmer reports hash-verified RGB frame-index timestamp/skew checks for all current pair and ROI rows, plus explicit ROI ordered raw indices with digest/count/bounds and exact per-scan union comparison; its self-tests include the reviewer frame-614 and digest-forgery attacks. Reported 11 complete-presentation and nine technical tests pass; independent verdict pending. The P2 rewrite removes the inherited shots 6–7 trajectory rewind and stabilizes ROI track order, but admits remaining map/orbit camera resets, stepped ray appearance, and no end-of-film hold; a subsequent motion-polish candidate is planned.

G02-P3 moving-overlap R4 is assigned to a separate Sol/high programmer on a new worktree from the approved-source documentation checkpoint `8ca6c00`. The narrow revision will use real RGB source frames 540–548 across the outgoing half of the frame-540 fade, instead of cloning frame 539 while M1 is still walking; it must preserve 12 shots, 1,350 frames, seven reviewed blend windows, legacy diagnostic behavior, and exact editorial/post-roll source timestamps. Other transitions retain their current sampling policy. This is assigned work, not an approved fix; exact source and rendered boundary review remain required.

Independent G02-P2 R4 verdict: **APPROVE**, exact commit `1b393cc1cfeea45e36a2a0c457a9e56f42d8113b`, tree `360facf85b6f323108f5ab82c11b79b1c0adf0b7`. The reviewer independently rejected forged frame-614 current/ROI timestamp pairs, forged ROI digest, descending/duplicate/out-of-bounds raw indices, and per-scan union mismatch. The positive fixture binds a real 615-row RGB index; the historical diagnostic resolves its actual 613-row index and remains delivery-ineligible. All 36 focused presentation/technical-view tests passed. A separate combined trial from documentation checkpoint `8ca6c00` is now assigned to the P2 programmer to resolve P3/P2 presentation test conflicts and run combined tests; final G02 source catalog and fresh capture are still pending.

The P2+P3 combined trial is frozen for independent review at merge commit `6318028b3f9721b6b4d9263f1725625ea57fe249`, tree `3c1f400c8248c807039da02c11e045e08a58d303`, parents documentation checkpoint `8ca6c00` and approved P2 R4 `1b393cc`. Only `tests/presentation_fixture.py` and `tests/test_presentation.py` needed manual conflict resolution; the programmer reports that they preserve P3 moving/time-labeled transition fixtures and P2 real scan, RGB index, ROI, and adversarial validation. Reported merged tests: 30 presentation, nine technical views, five scan projection, all pass; tracked worktree clean. This is a submitted combined state, not integration approval. A separate P2-R5 motion-polish branch from this frozen trial is assigned for camera continuity, smoothly introduced activation rays, and an eased final hold, with CPU trace and diagnostic boundary review required.

Independent P2+P3 combined verdict: **APPROVE** on `6318028b3f9721b6b4d9263f1725625ea57fe249`/tree `3c1f400c8248c807039da02c11e045e08a58d303`. Both approved parents and source ancestry were verified; all six P2-only blobs and 183 P3-only blobs were byte-identical to their approved parents. The two resolved test files preserve both contracts. A broad exact-tree suite covered 101 tests; one hit a OneDrive `os.replace` lock after rendering, then passed alone with `TEMP=C:\Temp\...`, so all 101 passed across the runs. The isolated integration branch fast-forwarded to this exact approved source; local journal/report edits remain uncommitted. This approves source combination only, not fresh G02 capture or final imagery.

Scene R2 provisional commit `0bf8b0cf3c0b488a9240e319bd0a2118365813f8`, tree `bf90313b3405deaf4af7eb94c7d0af42c8112287`, produced r2b 720p lookdev images. Direct and independent early visual triage agree they are materially brighter and show real stocked endcases instead of the old colored proxy, but still fall short of photoreal RGB: nearly continuous white under-shelf lights read as wires across products, the 17-second refrigerator/endcase remains sparse/dim with weak glazing and a blank header, the 9-second right-side display blocks a quarter of the frame rather than presenting readable packages, and surfaces remain repetitive CG. The r2b manifest identifies base `e0ceb30d` and `dirty=true`, so these images are diagnosis only, not exact-commit acceptance evidence. Findings were routed to the scene programmer for another revision and clean exact rerender.

G02-P3 moving-overlap R4 is submitted for fresh exact review at `86b65f0c70080d016d2ca20d6f3d05705749363b`, tree `41b9f232488845372af86b19e9be178b17b802c1`, based on documentation checkpoint `8ca6c00`. The programmer reports shot 5 now trims real RGB source frames through 548 with no outgoing clone, while shot 6 retains its incoming nine-frame pre-boundary hold; the total remains 12 shots, seven smoothstep windows, and 1,350 frames. Complete schema v4 requires a hash-bound contiguous RGB video/index through 548 and records actual post-roll frame IDs/timestamps, with technical measurement time null and editorial/noncotimed classification. A 640x360 two-shot synthetic moving boundary clip/strip was visually inspected by the programmer; 35/35 presentation/post-roll tests pass. This is submitted code and fixture evidence only, not final capture/film acceptance; independent exact review is in progress.

Independent P3 moving-overlap R4 verdict: **APPROVE** on exact `86b65f0c70080d016d2ca20d6f3d05705749363b`/tree `41b9f232488845372af86b19e9be178b17b802c1`. The reviewer ran 35/35 presentation/post-roll tests and rendered a direct 1,350-frame numbered fixture: the candidate advances actual RGB frames 540–548 where its parent repeats 539. The manifest binds video/index hashes and observed 531–548 stamps, while technical measurement time remains null and all other six transitions retain their reviewed edge-clone behavior. No capture/SLAM/motion/perception source was changed and the legacy diagnostic plan remains byte-identical. A separate P2+P3 combined trial from current integration `6318028` is assigned; fixture approval does not establish final production RGB or technical visual acceptance.

Scene R3 `60f7fc0e1317b066870f55927f6a64eebfeba937`, tree `05e518004a8d33c78653fa4af68500163c7784f7`, now has a clean exact native-production-config 1920×1080 CaptureOnly frame at source frame 539/9s. The image SHA-256 is `462dc07c80ee5a8270919f65fbd09369cde3db7aa0a5a20874060509241d1823`, manifest `a2d0e452092ec12d200eaffc065fcf881e72a492a9c2f83c30b6978eac60268e`, status `f90ca82158505381c51340a51d11a8ec9ae0c63952d4f7485f9afc5942e01487`. Provenance records source clean, production scenario/sensor hashes, 1920×1080, and 90-degree horizontal FOV. The run exited zero in 227 seconds and 33 focused scene/capture tests pass. I inspected the exact frame: the bright shelf-wire defect is gone and the right display is readable, but flat regular structural surfaces and weak material/contact detail still read synthetic. Independent exact R3 source/visual review is in progress; CaptureOnly is not ROS/RTX delivery evidence.

Independent exact G02-S R3 verdict: **OVERALL REQUEST_CHANGES** on `60f7fc0`/tree `05e5180`. The reviewer accepted capture hardening source in static/CPU scope (71 passed, two skipped): explicit native 1080/90-degree calibrated scenario, six required topics and positive counts, nonempty LiDAR, absolute 20.5-second horizon, 600-second cold startup versus 60-second progress stall, and fail-closed `CAPTURE_COMPLETE`. Actual clean 3/9/17-second camera frames still look stylized: uniform modular gray shelves, flat package art, sparse/even facings, repeated ceiling/floor/light rhythm, weak glass hardware/reflections, and no tight oblique merchandise composition or store-use context. R4 visual changes are routed to the scene programmer; final ROS/RTX full-run acceptance remains open even for the source-approved capture gate.

P2-R5 technical-motion candidate is frozen for exact review at `0f191e6b932d395e446261623b93bf81d21a4c0d`, tree `d4523844b6d254ac19ca27fcd24bdb6b54972af2`, parent approved combined `6318028`. The programmer reports a shared continuous estimated-map camera path through technical shots 6–12, quintic bridge, fixed primary ROI focus, staggered ray opacity, and exact 24-frame final hold; a hash-bound 810-row eye/target/velocity/acceleration trace and historical diagnostic boundary strip are available. Reported 48 focused tests pass. Two real historical strip attempts stalled on 15.8 GB archive I/O and are explicitly unavailable; fresh current-goal capture and final rendered-motion evidence remain required. Exact independent review is queued.

P3 moving-overlap R4 merged into an isolated P2+P3 trial `c70ef31d8e91c7b4fe8864124cd79f6177136499`, tree `f9bca9047fef63a56e56bbb2aa2587e2fa915147`, parents approved integration `6318028` and exact-approved post-roll `86b65f0`. Only `tests/test_presentation.py` needed combined resolution, preserving P2 and P3 adversarial/transition cases. Reported combined 51 presentation/post-roll/technical/scan tests pass; independent exact combined review is in progress. The integration branch has not yet moved to this trial.

Independent P2+P3 moving-post-roll combined verdict: **APPROVE** on exact merge `c70ef31d8e91c7b4fe8864124cd79f6177136499`, tree `f9bca9047fef63a56e56bbb2aa2587e2fa915147`. The reviewer verified seven P2 production blobs and six core P3 production/test blobs byte-identical to their approved parents, the sole `tests/test_presentation.py` resolution preserving both causal-scan/ROI attacks and the new 531–548 post-roll mapping, and no capture/SLAM/motion/asset source changes. An independent combined run passed 106 tests with two documented pinned-zlib skips. The isolated integration branch fast-forwarded to this exact reviewed source; local journal/report edits remain uncommitted. Final GPU/capture and film visual acceptance remain open.

Independent P2-R5 technical-motion verdict: **REQUEST_CHANGES** on exact commit `0f191e6b932d395e446261623b93bf81d21a4c0d`, tree `d4523844b6d254ac19ca27fcd24bdb6b54972af2`. The reviewer found three blockers despite 48/48 focused tests: the 810-row camera-motion trace and per-view motion receipts are not verified by delivery validation; object-detail and observed-aisle camera poses use estimated-trajectory times beyond their declared source windows (69 and 23 rows, respectively); and actual piecewise estimated-trajectory motion has eye/target acceleration spikes at frames 120 and 420 that the smooth synthetic test misses. Fixed ROI identity, staggered ray opacity, and exact 24-frame final hold passed. A separate read-only I/O audit found the technical RGB reader repeatedly backtracks FFmpeg for previous/latest pairing: shot 6 would restart decoding about 78 times and decode about 9,496 frames versus about 175 for a forward pass. The audit confirmed exact LiDAR CDR rows are indexed rather than a full-bag scan per frame; two full-bag SHA passes are deliberate and must retain integrity if optimized. All blockers and the decoder cache fix were routed to the same P2 programmer for an appended R6 and fresh exact review. R5 is not integrated.

Scene R4 is frozen for independent source/visual review at `1407eb58ce28ae99a96a9754ed0bd59f61819048`, tree `fb0b70c3c4a7de1c39fde3506978e73ca55fe1b6`. The worker reports 33 focused tests and a passing PowerShell parser; the capture wrapper now records/enforces a clean source tree, checks raw bag RGB image cadence and count against the RGB video, and retains the R3 native 1080 scenario. Clean exact 3/9/17-second 720p and 9-second 1080p frames are rendering; the R4 visual verdict and full live six-topic capture remain open. A read-only capture preflight identified 4.08 GiB as a conservative fresh 1080 bag estimate, 600-second cold startup and 60-second progress timeouts, and the exact `walking_production_1080p.yaml` launch path. The unreviewed R4 source is not in integration yet.

A separate 12-shot boundary audit found that the film frame-531–548 transition blends walking RGB around source 18 seconds with earlier technical sensor data around 2–5.9 seconds. Per-frame provenance already says editorial and non-co-timed, but the first technical title can imply live activation. A presentation-level constant-opacity label over film frames 531–659, identifying an earlier sensor replay and its source time, is assigned for implementation. It must remain visible from the first mixed frame after compositing; preserve all seven windows and 1,350 frames. This change needs exact candidate and combined review before final film acceptance.

An isolated source-feasibility trial merged current approved integration `c70ef31` with Scene R3 `60f7fc0` at trial merge `a896e5dfe150d0e7ec187d7fb80cc756ac7db980`, tree `0aae051bcc963944ccab6d85e83c427a3966e1af`, with zero conflicts. All 14 Scene R3 contributed paths match their source blobs and base presentation/technical/motion/assets remain byte-exact; 103 CPU tests passed with two documented zlib-dependent asset generation skips. This is compatibility evidence only, not R3 visual acceptance. A separate R4-on-current-source feasibility trial is assigned while exact R4 images and independent review proceed.

The Scene R4-on-current-source feasibility trial is frozen at merge `25ee8063dcb195dd4333f51bbb63d3a18cd81dd7`, tree `970979b03d0f8b1c480673a902e9a43776aa9ea9`, parents R3 trial `a896e5d` and exact R4 `1407eb5`, with zero conflicts. All R4 capture/visual blobs are exact to R4, and c70-owned presentation, technical/LiDAR, motion, sensor, and retail blobs remain exact. Combined capture/scene/technical/presentation CPU suites passed 83/83, no skips. This is source compatibility only, not scene visual approval or final ROS capture. An independent R4 reviewer verified the clean 3/17-second 720p frames and gave an interim visual blocker: symmetric gray shelves and flat packages remain stylized. The reviewer is awaiting the native 1080p 9-second frame and found capture-gate weaknesses to route into the next scene revision.

Independent exact Scene R4 verdict: **REQUEST_CHANGES** on `1407eb58ce28ae99a96a9754ed0bd59f61819048`/tree `fb0b70c3c4a7de1c39fde3506978e73ca55fe1b6`. The reviewer hash-verified clean 3/9/17-second evidence (720p frames 179/1019 and native 1080p frame 539) and found the symmetrical gray shelf tunnel, flat facings, weak floor/fixture/glass response, and missing close merchandise/store-use context still fail the visual goal. The native 1080 foreground package labels are horizontally mirrored; two hero facings and two baskets have measured geometry overlaps. Capture source checks were start-only and bag RGB duplicate stamps could pass current reconciliation. These blockers were routed to the same scene programmer for R5 and require fresh exact review. The clean native1080 frame SHA is `a8c652b119a392382262d2c9d8930e25ff9da55898bdc5c3dd33b7700394087f`; manifest `d6bccf1228c04b0c6a14411cfec014721c054cd1a7accd5234e5c9c255d8930e`. CaptureOnly images still cannot prove live sensors.

A full diagnostic native1080 ROS/RTX capture from clean isolated R4 combined trial `25ee8063` ran to the 20.5-second horizon and **failed closed** (wrapper exit 1) under `scene-r4-feasibility/runs/20260925-031038909`. Isaac simulated 1,230 ticks and observed 613 RGB frames, 204 real nonempty LiDAR clouds (15,860–21,834 returns), and 1,229 clock; raw bag had all six required topics but only 606 RGB image messages, CameraInfo 613, LiDAR 204, while the RGB video recorder stored 603 frames. Both raw bag and video had 66.7 ms RGB gaps, and bag max RGB/LiDAR skew reached 33.3 ms. Recorder actual cadence was 29.5098 fps despite simulation reaching the horizon; no `capture_manifest.json` or completion marker was published. CameraInfo observed fx=fy=960 and cx=960/cy=540 versus configured half-pixel cx=959.5/cy=539.5. This is delivery-ineligible runtime diagnostic evidence, not final capture. A read-only root-cause audit is inspecting subscriber/encoder throughput and slower-than-realtime pacing while the scene programmer prepares R5 without weakening 1080/30Hz gates.

Read-only capture root-cause audit established independent subscriber backpressure rather than Isaac sensor loss: CameraInfo and embedded runtime each have 613 frames; bag missed seven image timestamps and video missed ten completely disjoint timestamps. The bag callback synchronously serializes/writes each 6.2 MB image in a one-thread six-topic executor (depth 10), while the live RGB callback synchronously decodes/encodes (depth 5). The 20.5-second simulation already took about 34 seconds wall (~0.60×), so merely setting 0.5× offers little headroom; a recorded 0.25× pacing factor and removing duplicate live video capture in favor of offline MP4/index derivation from a fully validated raw bag are recommended. Bounded asynchronous bag writes and fail-on-overflow/high-water receipts are also recommended. The scene owner has the audit; material/texture work is assigned to a separate worker on material-only paths to overlap with capture/geometry repairs. No lossless 1080 capture has yet passed.

P2-R6 technical motion/trace/cache revision is frozen for independent exact review at `7b0d8d41f6f62eec6e0607b1c6486f92a23fb709`, tree `2e8a6068b2011f097bbf7b3325f683f55120105b`, parent rejected R5 `0f191e6`. The programmer reports delivery validation of the full 810-row trace and per-view receipts, separate camera-guide timestamp versus rendered-data cutoff, source-window contracts, smoothed real estimated path at frames 120/420, and four-frame RGB LRU. Reported 31 presentation and 21 technical/motion/projection tests pass. The historical trace/strip are diagnostic, pre-G02/pre-M1, delivery-ineligible. The independent exact reviewer is checking whether all R5 blockers actually close; R6 is not integrated or final visual evidence.

P3 replay disclosure candidate is frozen for independent exact review at `e318548f5c1112f2eb5ced0236bbf2f0eee29e63`, tree `fb9c6b61a5870ba03221ffe1912988ec07c81062`, based approved combined `c70ef31`. It adds the constant-opacity `EARLIER SENSOR REPLAY · SOURCE t=02.00–05.90 s` over film frames 531–659 after final compositing, visible even at blend weight zero, and changes the frame-540 intent to an earlier offline/non-co-timed editorial replay. The programmer reports 31 presentation tests passing and inspected generated-fixture frames 530/531/540/659/660, with seven windows, 1,350 frames, and legacy diagnostic unchanged. This fixture is not production RGB or LiDAR evidence; independent exact review is in progress.

Independent P2-R6 verdict: **REQUEST_CHANGES** on `7b0d8d4`/tree `2e8a6068`. Although the RGB LRU, replay title, and historical guide acceleration passed, the global Bezier fit depends on future trajectory beyond declared per-view windows; a self-rehashed +1 m translated or zero-phase trace still passes delivery validation; alternate trace path and 25-frame final hold bypass exact binding; and the smooth guide trace is not used to render current RGB shots 6–7. These four blockers were routed to the P2 programmer for R7. The reviewer passed 87 focused tests but did not accept final capture or motion evidence.

Independent P3 replay-disclosure R1 verdict: **REQUEST_CHANGES** on `e318548`/tree `fb9c6b61`. The lower-left post-composite label is visible from frame 531 but overlays the production technical footer throughout shot 6, hiding frame role, selected-return count, rigid scan/no-deskew notice, and source time. The synthetic fixture omitted that footer and missed the collision. The same programmer is preparing R2 in a non-overlapping reserved region with production-layout tests and fresh exact review.

Scene R5 CPU source `f3e7e748762526902d9123d868cf0f0e84e525ce`/tree `3094195ae1fd7c4e3c17257bfdbfbde8996cdfb4` merged conflict-free with approved c70 into isolated diagnostic trial `c1efd6cf64e6d999dd89f34e1a6ce47b82d98113`/tree `8fbf4950fff00346fbfb5f4c4c8048480df0e73f`; broad combined CPU tests passed 81/81 after a known temp-directory ACL retry. A full clean native1080 0.25× ROS/RTX diagnostic from that trial reached 20.5 s in 83.34 s wall, with embedded RGB/CameraInfo 613 and LiDAR 204, but again **failed closed**: bag RGB 589, video 600, 66.7 ms gaps, no completion marker. A second audit found 24 bag/13 video missing stamps with only one overlap and identified synchronous 6.22 MB callbacks and small default Zenoh fragmented-message buffers; deep subscriber queues and pacing do not repair loss before callbacks. The scene programmer is redesigning bag capture around a bounded raw-Cdr queue and offline video derived from validated bag, with source-controlled larger Zenoh buffers and short staged probes before another full run.

Independent exact Scene R5 SOURCE verdict: **REQUEST_CHANGES** on `f3e7e74`/tree `3094195` before visual render. Postrun source recheck, duplicate/strict count/cadence gates, explicit 0.25× timing, and UV inversion mechanism passed static/CPU review, but the new basket/hero objects intersect display back/base/shelves; 118 of 213 edge facings intersect shelf lights and two new price displays overlap existing tags. The R5 combined negative capture is valid evidence against byte-identical capture source, although not a positive exact-candidate acceptance. These blockers were routed to the scene programmer for an appended revision with cross-group SAT tests; R5 visuals remain unrendered/unaccepted.

Independent material-only R1 verdict: **REQUEST_CHANGES** on `431b9148e7497deeb549597c64e44f2673aec1e3`/tree `4aedb996945e43567bd582e4ee84b94cf617a247`. The 24 PNGs, manifest, two deterministic regenerations, normal/roughness ranges, tiling seams, and material preview passed source/visual checks; new case/rail/detail maps still require builder binding and actual RTX visual review. The sole blocker is the inherited `tests/test_scene_lookdev.py` assertion of nine old R4 material hashes, which makes the candidate's exact test suite fail. Material worker ownership was narrowly extended to that assertion block for an R2 manifest-based regression; geometry/collision test ownership stays with the scene programmer. Fresh exact review required.

Independent material R2 verdict: **APPROVE** on `2d4c9e5dfe74483513d54dc83d46db588f6edd81`/tree `9f3ef8e184c361d89c41b92fbf40748cf5a87e03`. The R1 maps/generator are byte-identical; only the old material hash block changes to require fresh deterministic regeneration, complete manifest/PNG bytes and hashes, semantic ranges, dimensions, and non-near-black shelf albedo. Seven focused scene tests and generator `--check` passed. This is source approval; new maps remain unbound until scene integration and require actual RTX frame review.

Independent replay-disclosure R2 verdict: **APPROVE** on `1b2e1399ded4a59c4f9e54039167f15c272a2a05`/tree `c6449d439662096ffd2e0c5d722a0b19d3baebb8`. A production-layout preview/delivery fixture verified zero badge overlap with title, three callouts, and technical footer at both 720p and 1080p; exact disclosure is visible only on frames 531–659, including zero incoming weight at 531, with all 12 extracted frame hashes and 96 manifest references matching. Seven windows, 1,350 frames, RGB postroll, and legacy diagnostic remain intact. The isolated integration branch fast-forwarded from c70 to this exact approved R2; final production film remains unrendered.

Scene R6 source is frozen for exact review at `69ca9d45969c0c06a3926b208c9ac5fce79c2b20`, tree `eac3282c18f3387f5686c07deafcb1ba9752cc04`, parent rejected R5 `f3e7e74`. The programmer reports all R5 SAT overlaps repaired, source-controlled Zenoh 16 MiB/queue-16 transport configs, publisher/subscriber depth 128, a bounded asynchronous serialized bag writer with fail-closed queue receipts, offline MP4/index from the accepted bag, exact RGB/CameraInfo/runtime stamp digests, and Frames-derived short capture horizons. Reported 43 focused CPU tests/compile/PowerShell parse pass. A separate exact-source reviewer is auditing it, and an isolated approved-source-plus-R6 180-frame native1080 ROS/RTX diagnostic is assigned before any full capture or visual acceptance. Approved material R2 will be integrated into the scene branch separately for actual RGB render.

Independent Scene R6 source verdict: **REQUEST_CHANGES** on `69ca9d4`/tree `eac3282`. The reviewer found the bag queue high-water receipt racy under immediate dequeue and the offline MP4 success gate satisfied by a nonempty but undecodable file. SAT geometry, transport configuration, and broad source contracts otherwise passed. An isolated clean R6+approved-integration probe (`f6bc17c32a43623797c3a1231d990d57026c9a6e`/tree `0398929acd74cd0f95dd9dee0458edce013d7b71`) passed 94 combined CPU tests and a **diagnostic** 180-frame native1080 ROS/RTX capture: 88 contiguous 30 Hz RGB/CameraInfo messages, matching runtime/bag/video timestamp digest `f4dc4d8...`, 29 real nonempty LiDAR scans, zero RGB/LiDAR skew, queue 565/565 submitted/written with zero overflow, and independently FFprobe-decoded 88 H.264 1920×1080 frames. The source review blockers prevent this short run from establishing delivery eligibility.

Scene R7 `af0bcb7b99178aa51f12722091bda82254f3a2a7`/tree `ae135d4663d2ea478bad77e9fa5bf9491502f3a8` independently **APPROVED for source only** after adversarial immediate-dequeue and silent-drop-encoder probes closed both R6 blockers. The exact material R2 assets are now integrated, bound semantically to floor/shelves, case frame/glass, rail, display wood, and category/end panels, with material manifest/hash in runtime source provenance. The real R6 H.264 artifact decoded as 88 frames through the configured ROS Pixi environment. An appended decoder switch to FFprobe at `c6d750d60be6a06c6451d5e76e1840cedbd2ed2f`/tree `74b0a5ae80d2fbe99702780d912c99db9d81723e` reports 46/46 focused tests and awaits fresh exact independent source review.

Clean exact Scene R7 lookdev manifests are `fe54e5b4473bfa739e01a97ae039f28e6bbc058c696df992b1fe20caff332457` (3/9/17 s, 720p) and `3dddd3573a6e70aa72cdd267f6b20d663e9333d3e7dc84923b53d25d514de3fc` (9 s, native1080), produced before the decoder-only append. Direct visual inspection found corrected foreground label orientation, better lit/material surfaces, and readable near packages, but the RGB still reads as a symmetric synthetic shelf tunnel with repeated facings, sparse store-use context, and dark flat case glass at 17 s. The scene owner is assigned an R8 composition/lookdev revision; source approval of R7 does not imply visual acceptance. A clean isolated full native1080 R7 combined diagnostic is staged separately to test the entire 20.5-second capture contract.

Technical P2 R7 `7d2cf67fec57fd9fc147271aef2ae01efcadc8c1`/tree `4f546d3238cfe43fef07efbcff4e8d17e255ae3f` is under independent exact review. Its claimed changes bind the 810-row motion trace to the actual source trajectory/transform/plan/inventory anchor, use recorded optical poses for current RGB shots 6–7, disclose the global 2–18.5-second camera-guide dependency, enforce canonical trace path and exact 24-frame hold, and retain an RGB decoder LRU. The historical strip/trace remain diagnostic and pre-G02. Do not integrate until the exact reviewer approves.

Independent exact Scene R7 decoder-append source verdict: **APPROVE** on `c6d750d60be6a06c6451d5e76e1840cedbd2ed2f`/tree `74b0a5ae80d2fbe99702780d912c99db9d81723e`, parent independently approved `af0bcb7`. The configured ROS Pixi FFprobe decoded the actual R6 H.264 artifact as 88 frames at 1920×1080/30 fps; a path with spaces was safe; corrupt, absent, and timed-out probes failed closed; a nonempty silent-drop encoder placeholder caused failed metadata and RuntimeError. The independent queue race probe retained high-water 1, pending 0, submitted=written=1; 24/24 capture/scene tests, material regeneration check, and SAT collision checks passed. This approves source only; full 20.5-second capture and RGB visual acceptance are still open.

Independent optional retail-cart R1 verdict: **REQUEST_CHANGES** on `291a25dbae3a7fda9c7a76b2baec56deb6a360a0`/tree `25750e6b485a61313c7ed1c425e3d8747fd583d2`. Its USDA quaternion serialization is rejected by bundled USD 25.11, all 13 collision proxies lack applied `UsdPhysics.CollisionAPI`, material bindings lack `MaterialBindingAPI`, and seven string-level tests miss these failures. Generator determinism, open-wire design, and approximate 1.028×0.600×1.028 m visual bounds passed bounded checks. The rejection was routed to its owner for a source-controlled R2 with actual USD stage/schema regression; R1 is not integrated or placed.

Independent visual review of clean exact `af0bcb7` R7 lookdev (3/9/17-second 720p, 9-second 1080p) returned **REQUEST_CHANGES** against the photographic RGB storyboard. Floor/ceiling/shelf maps and corrected near label orientation visibly help, yet repeated low-detail inventory, low-poly produce, blank shelf backs, uniform dark shelf framing, weak case glass/content, sparse price detail, and flat lighting remain. The user's prior direction removed the foreground shopper; current G02 objective does not request its return, so missing shopper alone is not treated as a new blocker. Exact `c6d750d` changed only offline video code but strict visual identity awaits exact-candidate frames or the staged full capture. R8 lookdev is in CPU development while the GPU is reserved for the full R7 diagnostic.

Full clean native1080 R7 **diagnostic capture PASS** from isolated combined trial `3dc5107e8fedee3835259282e1655749ec7438c0`/tree `048f7d9d50d6339864ac0bc9e4b2d3a4fbb7ccb4`, combining approved integration `1b2e139` and exact source-approved scene `c6d750d` without conflicts. Focused combined CPU suites passed 59/59 and 38/38. Run `scene-r7-full-capture/runs/20260925-041644489` produced a complete manifest and `CAPTURE_COMPLETE`, wrapper exit 0, archive validation valid, capture SHA `5429fb8cfbbae8bc5bca6c2c688515eb49e3b81c5f8eae2bad1de836730e86d`. Six topic counts: clock 1,228; RGB 613; CameraInfo 613; LiDAR 204; tf 1,230; tf_static 1. Raw RGB, CameraInfo, embedded runtime, and offline video share exact timestamp digest `15af92284e5ead0ccc1a8b5152812fc06c2ae7dbecd80c34471b4c9cf516fcbe`; first/last 0.1/20.5 s, max RGB gap 33.333335 ms, 1920×1080/30. Bag queue drained 3,889/3,889 writes, zero overflow/error, pending zero, high-water three. Independent FFprobe decoded all 613 H.264 frames. All 204 LiDAR clouds were nonempty with 16,061–21,878 returns, max 100.000002 ms gap and zero RGB skew. This proves source/capture mechanics at R7 but not RGB photorealism or final G02 delivery; the R8 scene change requires fresh physical capture. GPU was released for R8 clean lookdev.

Independent P2 technical-motion R7 verdict: **REQUEST_CHANGES** on `7d2cf67fec57fd9fc147271aef2ae01efcadc8c1`/tree `4f546d3238cfe43fef07efbcff4e8d17e255ae3f`. The declared per-view and global trajectory dependency windows exclude interpolation bracketing knots: shot 6 declares [2.0,5.9] but uses a 6.8 s source row; historical map guide uses 1.2 and 18.6 outside [2.0,18.5]. Mutating only excluded rows changed trace rows (nine shot-6 frames, 382 early-map and 598 late-map rows). The other R6 blockers closed under 89/89 focused tests, cross-runtime trace identity, bounded RGB decoder cache, exact 24-frame hold, and smooth historical motion. The sole blocker is routed to a P2 R8 programmer for support-row disclosure/binding and sparse-knot regression; do not integrate R7.

Independent full-capture trial integration SOURCE verdict: **APPROVE** on exact `3dc5107e8fedee3835259282e1655749ec7438c0`/tree `048f7d9d50d6339864ac0bc9e4b2d3a4fbb7ccb4`. The two approved parents have zero changed-path overlap (20 integration paths, 42 scene paths); their 62-path union is byte-identical, with no extras. A detached-clone combined suite passed 71/71 CPU tests. Runtime capture and visual status remain separate.

Independent optional retail-cart R2 SOURCE verdict: **APPROVE** on `a6dbd60e8d103ad43f740fe8a11f030fe7b95348`/tree `e2bc6b8c10ee3bbe0aeb98cdcb4076aa98d75f4a`. Bundled USD 25.11 opens the exact USDA; all 118 tube quaternions, 118/118 material bindings and 13/13 enabled collision APIs validate; eight parser-backed tests pass and fail closed without USD bindings; two generations match the committed outputs. This approves only the standalone 1.028×0.600×1.028 m cart asset. Placement, scene collisions, and actual RGB remain open.

Scene R8 clean CPU candidate `310349c891a40e2cbdadae1a5750a752641fb316`/tree `4d95841d3d19f042e3adb7cee7f991b38bf60664` adds eye-level right-side focus groups at 9 s and 17 s, varied dense facings spanning 48 keys, smaller hero sign, and refrigerator edge practicals/fill. The scene programmer reports SAT checks including a 0.42 m center-path envelope and 46/46 focused tests. Exact clean 720p 3/9/17 s and native1080 9/17 s lookdev manifests are `c48a8fcd799f542c97c80a95a1975e91cf4d5d79110024cc549684676389588a` and `d0edd912546b1e57b303b23eace138ebacc61b4e626a3dc879723d140deacb41`. Direct inspection shows clearer right-side packages/prices and brighter case content but still a central corridor occupies most of the 9/17 s views; this is not accepted as photoreal. Exact source review is in progress. A separate M2 camera-direction programmer is assigned to smoothly aim the recorded rig toward those right-side groups without breaking pose/TF truth or corridor clearance; scene R8 remains frozen pending that integration and actual preview.

Independent exact Scene R8 SOURCE verdict: **REQUEST_CHANGES** on `310349c891a40e2cbdadae1a5750a752641fb316`/tree `4d95841d3d19f042e3adb7cee7f991b38bf60664`. A generated `dense_price_r0_b17_l0` intersects new `late_focus_plinth` by 6.38 mm XY and 3.30 mm vertically; existing 46 passing tests did not compare dense price refs with new context structures. Other SAT/determinism/corridor checks passed (48 unique asset keys, 263 dense refs, min center clearance 0.507/0.542 m >0.42 m). The finding was routed to the same scene owner for R9 and an expanded collision regression; changed candidate requires fresh exact source review. R8 visual was already deemed insufficient due centered camera framing, prompting separate M2 motion direction work.

R7 diagnostic offline SLAM was started on the accepted full capture to expose end-to-end runtime issues before the R9/M2 final physical recapture. It has stalled before bag replay despite two visible RTAB-Map nodes; the operator is investigating a bounded wait condition/process tree and will preserve a failure report if it cannot proceed. This is diagnostic only and cannot promote the R7 scene to final acceptance.

P2 technical-motion R8 candidate `67e955ac0b98efb4d9534a8c37e52039cb67f387`/tree `ff59c81b19a903c594f3d91848095c79639a5bfa` is submitted for fresh independent review after rejected R7. It binds direct/global camera guide declarations to the actual interpolation-knot envelopes (historically shot 6 [1.2,6.8], shot 7 [5.6,9.2], map [1.2,18.6]), rejects false prior ranges, and adds sparse-knot mutation regressions. The programmer reports 10 motion and 31 presentation tests passing, with the 810-row historical trace byte-exact. Source approval and final current-run rendering remain open.

Scene R9 source candidate `c5fa6aaea67d98c3decce30ddcb8caa0a5d30388`/tree `69d0bdb0368fa105a2d8f4762cbb5ad3a7970923` is submitted for fresh exact review after R8 rejection. The dense generator now rejects any facing/price whose full 3D SAT volume intersects any authored context structure; the previously overlapping price is suppressed. Programmer reports 262 dense refs, 49 prices, 48 keys and 12/12 scene tests. Visual framing remains open and separate M2 full-rig camera direction is in implementation.

The R7 diagnostic SLAM process was safely terminated after 7m46s without reaching observer/player replay. Two mapper nodes and requisite topics were independently visible, but `scripts/run_slam_offline.ps1` blocks on an inline `pixi ... ros2 node list` invocation before its outer 90s deadline can fire. Partial database only, no accepted SLAM or perception outputs. A bounded per-probe timeout/child-cleanup source repair is assigned; fresh candidate review and retry are required. Capture archive remains unchanged and valid.

Independent Scene R9 SOURCE verdict: **APPROVE** on `c5fa6aaea67d98c3decce30ddcb8caa0a5d30388`/tree `69d0bdb0368fa105a2d8f4762cbb5ad3a7970923`. Exact R8-to-R9 set difference removes only the offending dense price; 262 refs/49 prices/48 keys remain. Independent 3D SAT checked all 262 refs against 695 context boxes (182,090 pairs) at 1e-5 threshold with zero intersections; all other pair, shelf, light, and corridor tests pass. 46/46 focused CPU tests and three-seed deterministic digest `5306d5e85a185e8e32d913118a29e72166d76b285d77e0067defe26ac0d32082` passed. This closes R8's source collision; centered R8 visual still fails the photoreal target.

Independent R8 visual diagnostic agreed the new eye-level groups improve label readability but not the central-corridor composition: 9/17 s camera yaw is only about 0.35°/1.76°, with right groups edge-cropped. Generic vector art, sparse merchandising, heavy black rails, flat shelf backs/floor and weak glass remain. Separate M2 full-rig rightward camera beats and an eight-SKU hero-art candidate are under review/visual integration. The older owner direction removed the shopper; its absence alone is not a blocker.

M2 camera-direction candidate `b01c4be7ca691d7629a77b06b530a2248b5164e2`/tree `03c7b386d49d4bb7504d546e2b797361f0973876` is submitted for independent exact review. Its source path, camera rig, and recorded truth/TF use the same eased rightward looks around 9 and 17 seconds, with 210 reported tests. A clean R9+M2 visual trial `54973af61e071c3c21d210466eeb764cb40b1412`/tree `81ab67ad139302826077549a255008e59f0ba9f6` rendered actual 720p and native1080 3/9/17-second frames. Direct inspection found materially better readable oblique shelf composition and foreground prices inside the frame. This is still stylized RGB; the native1080 17-second frame exposes a mirrored STONE PIZZA label on the right shelf. Scene R10 is assigned to repair its physical orientation/asset; M2 and the trial remain frozen for review. The trial still needs temporal review, and neither a still nor source approval establishes final visual acceptance.

Hero packaging R1 candidate `4b365b51ee5c32a4bc075b3589b0289c5f05957c`/tree `f6be9f612e4bcb6c57a3a9dc1ec8724a58cec2cf` added printed side/back panels for eight existing hero keys, preserving 79 keys and 45 new geometry profiles, but independent source review **REQUEST_CHANGES**: 24 new side/back print meshes have inward geometric winding against authored outward normals, and back UV mirrors copy. The finding was routed to the same programmer for R2. Four original photoreal transparent food cutouts (oat/berry bowl, toasted cereal loops, citrus, roasted coffee scoop) were generated with the ImageGen skill and saved under `runs/production-quality-20260924/generated_food_assets`; the programmer is integrating them as source-bound original imagery behind separately generated copy. They are not yet accepted as actual RGB improvement.

Independent bounded-readiness R8 source verdict: **APPROVE** on `dd1bbc3aa08f5e9bd22061dd544c81c3a3a8e902`/tree `9f19d89fdbc17a9fe85e5d437e2714f29de91cd6`. Per-probe child timeouts and scoped cleanup let the strict R7 diagnostic SLAM reach mapping nodes, observer nodes, and bag replay. A new observer contract failure then emerged: first observed clock was 5.4564382 s, but its target incorrectly added the full 20.5 s span and required an impossible 25.9564382 s; live mapper database appeared empty even though graceful shutdown produced an integrity-valid database with 18 nodes and final stamp 20.5 s. The observer failed closed, so no SLAM/perception manifest was accepted. A separate R9 observer/launcher ordering repair is assigned; the valid R7 six-topic capture remains unchanged.

Independent P2 technical-motion R8 verdict: **REQUEST_CHANGES** on `67e955ac0b98efb4d9534a8c37e52039cb67f387`/tree `ff59c81b19a903c594f3d91848095c79639a5bfa`. Although it enclosed historical sparse interpolation knots, static historical supports could not describe the dense 20 Hz corrected trajectory expected from current SLAM. That would reject a genuine producer-shaped current run. R9 candidate `03051e69da4ace836e3a25badd0674ffa673a4f7`/tree `4286e6368127be9ec4434b18e4771bd648802057` now derives exact dependency envelopes from the hash-bound trajectory and independently validates the trace/receipt windows; programmer reports 10 technical-motion and 31 presentation tests, dense and sparse adversarial fixtures. Fresh independent exact review is in progress; no current-run technical frames are accepted yet.

Independent P2 technical-motion R9 SOURCE verdict: **APPROVE** on `03051e69da4ace836e3a25badd0674ffa673a4f7`/tree `4286e6368127be9ec4434b18e4771bd648802057`. Independent sparse and dense trajectory mutation attacks verified actual interpolation dependency windows, 810/810 trace rows, false manifest/per-view receipts rejected, cross-runtime identical canonical trace SHA `2de18b196faa4725cd567f62bf0c41eb59b0098a8442413fb02b0b4ac5abe8a3`; 90/90 exact focused tests passed. This is source approval only. The new dynamic camera-head direction now requires a separate time-varying transform consumer update before fresh RGB/LiDAR alignment can be claimed.

Independent hero packaging R2 SOURCE verdict: **APPROVE** on `c3ed3e4b1de1880c5a2c79fb97637eae8c92272e`/tree `f89f034ed3f99e783307cfbdae9ef13cc40b2431`. Bundled USD 25.11 opened all 79 assets; 24/24 added side/back meshes have outward geometric winding, correct UV direction and material binding. Four original food photos are pinned to the intended four hero fronts; 45 G02-A assembly/geometry profiles remain distinct; 23/23 exact tests pass. CPU sheets look cleaner, but actual GPU RGB is still required. A diagnostic visual merge with the scene and revised camera is assigned.

Independent M2 camera-direction SOURCE verdict: **REQUEST_CHANGES** on `b01c4be7ca691d7629a77b06b530a2248b5164e2`/tree `03c7b386d49d4bb7504d546e2b797361f0973876`. The full focus plinth extends below frame bottom (NDC -1.475 at 9 s and -1.380 at 17 s) even though product/price tops fit; the passing frustum test omitted the plinth body. More seriously, the full camera-LiDAR sensor rig yaws right while advancing down aisle, causing 34.15° sideslip at 9 s and >15° for 4.492 s at speed >0.5 m/s without a holonomic or head-articulation contract. The programmer is changing M3 to an independent camera head with base/LiDAR travel alignment; downstream RGB perception and technical LiDAR consumers are separately assigned dynamic transform handling. Independent review of M2 actual stills confirmed better oblique composition but plinth crop, mirrored ACTIVE HARVEST labels at 9 s, inverted STONE PIZZA at 17 s, arbitrary mixed product groups/price tags, and stylized materials. Temporal motion remains unreviewed.

Scene R10 source candidate `955fa03b8690343ebe3462bcc09d3dd3a5abda01`/tree `2dac35bec727b0fc491632679528d5e050f8ef88`, parent approved R9, limits a local-X display reflection to frozen_pizza references and adds two-row/scale regression. A clean diagnostic M2+R10 native1080 17-second frame shows STONE PIZZA and EMBER STONE left-to-right with no obvious culling or geometry shift; independent exact source review is pending. The scene programmer is also checking the separately mirrored ACTIVE HARVEST label at 9 s. The M2 diagnostic camera source remains rejected; final appearance needs M3 recheck.

An isolated final-production trial branch was staged from independently approved R7 combined `3dc5107`, then merged approved scene R9, approved P2 R9, and approved hero-art R2 without Git conflicts. It is not yet the final source: R10, M3, dynamic transform consumers, and SLAM observer repair are pending. Scene tests passed 12/12 after the R9 merge. Combined P2 technical-motion/presentation tests passed 42/42 after the P2 merge under the configured Isaac Python runtime with temporary-fixture filesystem escalation; earlier sandboxed attempts failed solely on temporary-directory ACLs. An independent review of the eventual exact combined tree remains required.

Independent Scene R10 SOURCE verdict: **APPROVE** on `955fa03b8690343ebe3462bcc09d3dd3a5abda01`/tree `2dac35bec727b0fc491632679528d5e050f8ef88`. Exact scope is an idempotent local-X reflection for frozen_pizza references; 40 instances across both rows preserve position, absolute bounds, collision and semantic IDs. Native1080 3/17-second M2 diagnostic showed upright left-to-right labels and no visible culling. The superseding Scene R11 `0b783212470db39e5220cff5889e89a48daff744`/tree `499d238086294fad32281430d6fce48148be1f4d` is also independently **APPROVED**: adds only sports_drink to the reflection allowlist, 100 instances across both rows preserve bounds/semantics, 77 other keys unchanged, 35/35 exact tests pass, and the clean native1080 9-second diagnostic shows ACTIVE HYDRATE upright. M2 remains rejected, so both visual frames remain diagnostic. R11 merged cleanly into the isolated final-production trial; combined scene/retail tests pass 36 with two documented pinned-zlib skips.

SLAM observer/launcher R9 exact candidate `04bc9edd6b72b1705ef370d1e8a52de6edd7d16d`/tree `75eaf85e6938dab725ace287c060680e235205f5` fixed the late-first-clock target and post-shutdown DB ordering, and 87 programmer tests passed, but independent SOURCE review **REQUEST_CHANGES** on two retry-integrity defects. A previous `rtabmap.db` is reused by default; the actual R7 diagnostic already has 18 valid nodes through 20.5 s, so a new failed mapper could appear successful. A previous complete `slam_manifest.json` also survives an early or late failed retry. The same programmer is assigned R10 with a provably fresh per-attempt DB, stale-manifest invalidation/atomic publication, adversarial seeded-old-state tests, and new independent review before R7 replay.

M3 articulated-camera SOURCE candidate `bf640e3732b464a646c52d44343868e420772ba3`/tree `89516f4d85e189546856018fbe8fbe2ba0f5de80` is frozen for independent exact review after M2 rejection. Its base/GT/LiDAR rig keeps travel-aligned orientation (maximum sampled horizontal sideslip 13.358°), while a dynamic camera_link head produces the 9/17-second rightward looks. USD and `/tf` publish the moving head; camera_link is removed from `/tf_static`. A 616-sample `camera_head_transforms.json` is SHA/size bound by `sensor_transforms.json`, and production receipt compares observed RGB stamps to the artifact. Source reports 27/27 focused and 213/213 full tests. The plinth's lower body is now an explicit intentional foreground crop; all products and complete price rail are required in frame. A clean native1080 diagnostic trial with scene R11 and hero art R2 is being rendered. Camera source approval, actual optical-frame visual quality, and downstream dynamic-transform consumer acceptance remain open.

An initial exact combined SOURCE audit of provisional final-production trial `f2c9fb9cc5dab7e00ce479ba044d912a1b6b1fb3`/tree `1313834c7d2690c3fd9b2472d160d1e65517cfbc` returned **REQUEST_CHANGES** on one stale capture-architecture regression. Hero R2 correctly changed cereal_sunrise's front from full-face UVs to wrap-atlas UVs, while `tests/test_capture_architecture.py` still searches literal old UV bytes before testing selective invalidation. The detached combined suite passed 119/120; the one test failed before proving its intended invariant. Independent remerge verified no manual conflict-resolution delta; all 79 keys, 45 distinct new geometry profiles, 2,017 deterministic placed assets, 115 structural references and 262 dense refs remain. The hero programmer owns a narrow test update and new exact/combined review. No final source acceptance or film is claimed.

First M3+R11+hero GPU diagnostic found another source regression before frames: standard representative-capture mode writes its status within the frame output directory, while M3 prewrites `camera_head_transforms.json` there before the recorder's empty-directory check. The visual worker is rerunning with a sibling status/artifact directory for diagnostic pixels only. The exact M3 reviewer was notified; normal-path repair is required before production acceptance. A separate optional-dynamic-artifact verification gap in strict capture validation is also under review: the capture manifest enumerates the head file, but its fixed required-path verifier does not currently rehash that extra file. Downstream consumer verification remains mandatory.

Independent M3 SOURCE verdict: **REQUEST_CHANGES** on `bf640e3732b464a646c52d44343868e420772ba3`/tree `89516f4d85e189546856018fbe8fbe2ba0f5de80`, with three exact blockers. `sensor_transforms.json` exports `sensor_rig→camera_link` as both a static transform and a dynamic artifact; current RGB tracking silently uses the stale static edge. Sensor-only archive validation accepts a deleted camera-head file or a rehashed descriptor path escaping capture, because it only verifies fixed required paths. Standard representative capture fails its empty output-directory startup because M3 prewrites the head file there. Independent 58/58 focused tests and numeric checks of 616 samples, 613 normal RGB-stamp matches, 13.358° max base sideslip, and optical-world pose passed. The M3 programmer owns M4 corrections; the P2 consumer owner is coordinating the disjoint graph and hash-bound projection.

SLAM retry-safety R10 candidate `9d3a8d85b1d6bb8306f2bcde7ab48a1048163cc1`/tree `027c18a12235692281676623faa77221e263e6f7` fixed R9's stale database and manifest reuse with a fresh attempt-specific mapper DB, prior-output rotation, post-shutdown validation, and atomic final manifest. Independent review **REQUEST_CHANGES** on one path-safety blocker: unvalidated `ExperimentName` may contain `..` and direct the new rotation outside the run's SLAM directory. Focused 25/25 reviewer tests passed but lacked the traversal attack. Same programmer owns R11 one-segment/containment validation and sentinel regression. R7 diagnostic replay remains pending.

SLAM R11 `255136d1906d62a138b88ce41922e01cae200f6a` fixed lexical traversal and unsafe experiment names, but independent review **REQUEST_CHANGES** because a Windows junction at `run/slam/named` redirected prior-artifact rotation outside the run. The launcher owner submitted R12 `4f0b2d1acf7cb7d4b2a47ebf69f8b96325196bf1`/tree `a8d280a895003be8ccdb96b4f757a32e23741371` with reparse checks before creation and rotation, junction sentinels, and 39/39 focused tests. R12 exact review is pending; a strict R7 diagnostic replay on the retained capture is in progress to expose further runtime issues without altering capture bytes.

Clean M3+Scene R11+Hero R2 native-1080 diagnostic merge `3f9c76a1cfee1f6eba438723850a0302885f552f`/tree `208c7bc85830a00809532c2a016b703b8f1d68a1` produced 3/9/17-second frames and seven consecutive 9-second frames. Independent visual review found visible improvement in the 9-second original-food cutouts and smooth local 9-second motion, but the 17-second blank carton, repeated generic tags, mirrored nearby shelf labels, flat lighting and procedural look remained. Its sibling-directory workaround was diagnostic only. Scene R12 `520f5e6f080a5b8de621ae6f1908c9406e3f9186`/tree `1eb6aca6b2a3a45c8ac6dfc8fb4a2e28bb441dbe` replaced the 9-second island with four cereal fronts and the 17-second island with coffee/tea/snack fronts and four aligned thin tags; actual native-1080 diagnostic frames show no clipping. Independent R12 source review **REQUEST_CHANGES** because every tag still uses the same hard-coded price texture and scene tests check only row alignment. Scene R13 is assigned explicit SKU-to-price assets and association tests.

Hero R3 `391c70e03dcde094ba8689dd683358126c1a4832`/tree `1bcc47a153216aa81691c8fe4d3afe7e1a621ed5` narrowly updates the stale UV invalidation test for the approved cereal wrap atlas; independent exact review is in progress. The isolated provisional trial merged frozen M4, P2 dynamic consumer, Scene R12, Hero R3 and SLAM R12 candidates without conflicts. Before SLAM R12, its broad combined suite passed **252 tests, two pinned-zlib skips**. This trial is provisional because each new component and the final integrated tree require separate approval and a new capture.

M4 camera producer `fc22956942bb6ea64c92ee64a90f20d6670e725e`/tree `2a58b5d5b3099fb4b7f72851c914645f9a39e9d7` closed M3's duplicate static edge, optional-file rehash, and standard representative-path startup; clean standard-path GPU smoke and 218/218 programmer tests passed. Independent exact review **REQUEST_CHANGES** on three additional executable attacks: omitted dynamic descriptor/artifact accepted when the static edge is absent; duplicate/reversed/boolean RGB timestamps and coerced schema numerics accepted; stale capture seal and fabricated rehashed artifact source/calibration fields accepted. Camera owner is implementing M5 strict type/timeline/source/seal bindings with new independent review. No production dynamic capture is yet accepted.

P2 dynamic consumer R1 `f08242030bb12e70a2be62d0c55aa7fa003d5270`/tree `f2f804dc8c1daededd7cf35e593ecbbed0e91e0c` passed 86/86 focused tests and verified image-time projection/caches, but independent exact review **REQUEST_CHANGES**. Actual M4-shaped TechnicalRenderer fails by inverting `None`, and technical receipt validation still requires a deleted static optical-to-rig path; a two-edge alternate static route also bypasses the direct-edge disjointness check. The P2 owner is implementing timestamp-aware render/receipt camera transforms, full graph disjointness, and producer-shaped integration tests. No final selective technical frames or film are claimed.

Independent Hero R3 SOURCE verdict: **APPROVE** on `391c70e03dcde094ba8689dd683358126c1a4832`/tree `1bcc47a153216aa81691c8fe4d3afe7e1a621ed5`. Its sole edit changes one actual FrontPanel atlas U coordinate from 0.500000 to 0.490000 in the copied test asset, preserving selective appearance/RGB invalidation while geometry/SLAM hashes stay fixed; no production asset or generator bytes changed. The combined 252-test provisional trial includes the corrected test; a redundant direct rerun was stopped while spending excessive CPU time on repeated catalog hashing and is not counted as a separate pass.

The exact R12 SLAM diagnostic replay on retained full R7 capture reached clock 20.5144542 s, odometry 20.5 s, 18 fresh mapper nodes spanning 0.2–20.2 s and 117,338 map points, then failed closed before manifest publication. The observer waited for one second of global callback silence, but the mapper emitted 1,811 map→odom TF samples during the bounded 60-second drain. R13 is assigned a replay-input-specific completion gate that retains horizon, odometry, and later map/graph requirements. The capture archive remains intact; inventory was correctly skipped. Separate independent R12 SOURCE review also **REQUEST_CHANGES**: a deterministic directory-swap race after its last reparse check redirected prior DB/manifest rotation outside the run through a junction. R13 must establish durable mutation-path ownership and regression against that race before a new review/retry.

On 2026-09-25 the owner explicitly requested that the current project work be pushed to `master` for review. Local master now includes the handoff/storyboard inputs, the provisional G02 trial through Scene R13, and WIP checkpoints `32f7d7e` (M5 capture validator), `d4a3d48` (dynamic technical consumer), `65ec488` (SLAM replay/lease work), and `d30f26f` (Hero R4 original food artwork), plus the independently approved standalone shopping-cart asset. These WIP commits were interrupted before exact source review and are not promoted to accepted production status. This Git checkpoint excludes large generated runs and temporary scratch. No current-scene paired production capture, completed SLAM/perception, accepted seven technical clips, or final 1,350-frame film is claimed.

## 2026-09-25 master continuation — checkpoint `6f24f38` (pre-documentation)

At the preceding published code checkpoint, remote `master` was `7270528eb85dded4ba4ff559cd04bd4e882af894` (tree `db6b966a8084537268fe3c9a26c9061b08b46009`). That owner-authorized push included the independently approved M5 strict-RGB fixture correction (`ba9a175`), the recovered snack-wafer normal/roughness files (`5a8fbe5`), and approved Hero R4 R2 source/generator repair (`43461f6`). Hero R4 R2 fixes the stale direction expectation and computes a canonical JSON provenance digest from the pinned repository source, so LF/CRLF checkouts and scratch-root regeneration agree. Its exact retail suite passed 23 tests with two documented skips. The R1 candidate was rejected and was not merged. A following documentation checkpoint `210885f` was later pushed before the current local continuation.

The full integration suite then exposed two stale fixtures: the complete-presentation fixture omitted strict observed RGB receipt fields, and one scene test resolved the original storyboard under an old OneDrive parent. The presentation fixture repair (`adafab6`) and storyboard path repair (`7270528`, independently reviewed candidate `d9651f3`) are integrated. The exact integrated checkpoint passed `C:\isaacsim\python.bat -m unittest discover -s tests -q`: 268 tests passed, two expected skips, in 170.273 seconds. The sandbox denied test temporary-directory creation, so this validation used approved filesystem access.

The corrected SLAM R13 R2 candidate `88d117d9ebf935708d6aa6efd169825b9c125ac3` (tree `26eb43c787610dcad393d11c10f694547d795683`, based on `5a8fbe5`) adds a pre-creation run-directory lease and a deterministic absent-output junction-swap test. Its exact focused launcher/capture suite passed 38/38, and independent exact source review approved it with no findings. The earlier R1 candidate was rejected and was not merged. No full R7 replay was run.

The complete reviewed R13 R2 series (`db58118`, `bb1d9dc`, `88d117d`) is now integrated on local `master` along with the earlier approved source and fixture repairs. The combined code checkpoint `6f24f38edf9f009db56f44a858f3e49565df6e85` (tree `d1280efd3fdf6dab86f07f316f0350ad544b0947`) passed `C:\isaacsim\python.bat -m unittest discover -s tests -q`: 269 tests passed, two expected skips, in 173.174 seconds. This includes the previously missing R13 lease implementation and regression cases. The final integration tree `734737dea0a330ed1a940bc528822a8d951af3f9` (tree `b62948623ba6f467cfaae6640bf3114e43a6fe89`, base `210885fcc1a7e9d013c2fdae5442de030c28c629`) was independently approved with no findings; code blobs are identical to the full-suite checkpoint. The owner-authorized push to `origin/master` succeeded (`210885f..734737d`). No R7 replay was run.

Production delivery remains incomplete: no fresh current-scene paired RGB/LiDAR capture, completed SLAM/perception run, approved selective LiDAR technical clips, or 1,350-frame final film exists. Hero R4 source review does not establish final visual realism; M5 capture-validation and dynamic technical-consumer source acceptance, Scene R13 visual acceptance, the combined exact review, and the production outputs remain open. Generated runs and temporary review scratch remain excluded from Git.

## 2026-09-25 SLAM GetMap/PublishMap identity repair

The sealed diagnostic `runs/20260925-183307101` exposed two separate runtime facts on source `48de461b42d5e0945b21432cef9f5523cc5b0874`: replay rate 0.25 silently lost synchronized mapper inputs, while rate 0.1 processed the exact 204-node LiDAR sequence with stamp SHA-256 `5aab0707589b38fffb6fd9f79170d5723c81b175615a9e027a44435c5ccd92ad` and the complete 203-edge directed Neighbor chain. The 0.1 attempt then failed only because RTAB-Map independently optimized the same stored source graph in GetMap and PublishMap, producing different optimized content hashes. Pinned CoreWrapper behavior confirms that exact optimized-pose/map-to-odom equality across those calls is not a service contract.

The implementation now compares the two calls by a canonical immutable source identity over positive node IDs, exact node timestamps, raw stored NodeData poses, and every canonical link constraint including its 36-value information matrix. Optimized poses, map-to-odom, and publication headers remain outside that identity and are recorded as separate pre/final forensic hashes, with total link counts and type histograms. The settled post-PublishMap `/mapData` and `/mapGraph` cohort is authoritative for the final optimized pose, dense trajectory, and map versions. A fresh cloud must share its post stamp and map frame; absence of a fresh cloud permits reuse only when the cached cloud's previously paired graph fingerprint is byte-canonically identical to the final graph. Downstream RGB and inventory provenance consumers enforce the same source and final-cohort contract. The offline replay default is now 0.1 while retaining the existing validated override range.

The initial R1 independent review requested fixture corrections because four existing test fixtures still encoded pre/final optimized-hash equality or omitted the new cohort fields. Revision R2 updates only those existing fixtures/assertions: valid cases now carry distinct pre/final optimized hashes, one equal immutable source identity, source link metadata, exact graph fingerprints, and coherent fresh or cached cloud state; the pose-loader case accepts changed optimization content for one source and rejects changed source identity. The localized-perception fixture now proves propagation of both source identity fields.

No new test was added and no full suite was run. The first sandboxed focused attempt could not create temporary directories and was terminated cleanly during the observer timing case; it is not counted as behavioral evidence. The first approved-filesystem run passed 11 intended cases but returned nonzero because the technical-view selector used the wrong pluralized class name; the corrected technical-view case passed separately. A final clean combined run of the exact 12 focused perception, inventory-provenance, observer, and technical-view cases passed in 4.338 seconds. Static Python AST parsing, PowerShell parser validation, stale-equality reference search, and `git diff --check` also passed. Fresh exact-source independent review and a new strict replay remain required; this entry does not claim either approval or runtime acceptance.

The next strict replay reached the complete exact post-PublishMap cohort and passed post-shutdown database validation, then PowerShell finalization stopped because the pending observer JSON had never declared three properties that the launcher fills after that validation. This narrow follow-up declares `database_node_stamp_sha256` and `database_node_timestamps_strictly_increasing` as null, plus `database_scan_coverage_complete` as false, in the initial receipt so `ConvertFrom-Json` produces mutable properties for the existing assignments. No cohort gate or mapping logic changed. The exact existing observer close-receipt test passed in 3.025 seconds with approved filesystem access; Python AST parsing, PowerShell parser validation, and `git diff --check` passed. No full suite was run. Fresh exact-candidate independent review remains required before replay.

A later strict replay exposed intermittent delivery of RTAB-Map's reliable/volatile KEEP_LAST(1) `/mapData`: the first attempt received fresh `/mapGraph` and cloud callbacks after an acknowledged `PublishMap(false)` request but no fresh `/mapData`, while a separate retry produced the complete exact triplet. The observer now makes at most three identical requests within one total close timeout, snapshots the three pre-request counters independently on every attempt, and records per-attempt acknowledgement, before/after counts, freshness, stamps, frames, graph fingerprints, raw cloud payload fingerprints, source identity, and acceptance. It accepts only a wholly fresh same-stamp/map-frame triplet whose `/mapData.graph` and `/mapGraph` fingerprints match and whose immutable source identity still matches GetMap. No stale `/mapData` or cached cloud is accepted. Repeated frozen-simulation stamps cannot combine differing deliveries: any graph or raw cloud payload fingerprint drift across attempts fails closed. The existing observer regression drops `/mapData` on attempt one and succeeds only after the identical graph and cloud arrive with a wholly fresh triplet on attempt two; its final isolated run passed in 4.741 seconds with approved filesystem access. A companion source/config test failed before reaching its observer assertions because the worktree's pre-existing packaged RTAB-Map config differs from the root config; it is not counted as evidence for this change. Python AST parsing, PowerShell parser validation, and `git diff --check` passed. No full suite was run. A fresh exact-source review remains required before another replay.

Independent review returned **REQUEST_CHANGES** on `2866651e48467f55ac978e0269fbc358ff06c666`: PMR-001 identified the invalid cross-attempt optimized graph/raw-cloud equality requirement, because independent PublishMap optimizations may legitimately differ while retaining one immutable source identity; PMR-002 required restoring the previously approved exact cached-cloud fallback. The revision discards every incomplete attempt as a cohort and compares graph fingerprints only between fresh `/mapData` and `/mapGraph` from the same request. A later attempt may differ. It can accept either a fresh cloud from that same attempt or the original pre-publish cached cloud when that cache exactly matches the attempt's fresh graph fingerprint, stamp, and map frame. A cloud from a failed attempt is never reused. The focused regression now proves a first attempt with different optimized graph and cloud bytes can omit `/mapData`, while the second attempt supplies its own fresh graph/data pair and uses only the exact matching pre-publish cache; three negative checks reject cached graph fingerprint, stamp, and frame mismatches. The final focused observer/RGB/inventory/technical-view run passed four tests in 5.686 seconds with approved filesystem access, but the latter three ran with concurrent unstaged perception changes present and are not evidence for this exact candidate. The exact observer regression passed independently in 4.729 seconds. No full suite was run. Fresh exact-source review remains required before replay.

A second independent exact review returned **REQUEST_CHANGES** with PUBRETRY-F01 and PUBRETRY-F02. PUBRETRY-F01 found that counter snapshots alone cannot identify a delayed callback from a prior request because pinned PublishMap has no request ID or barrier. The source-backed fence relies on all three run publishers being VOLATILE: a newly matched reader receives only post-match samples. Every attempt destroys and verifies the prior subscription cohort, advances an observer generation, creates generation-bound `/mapData`, `/mapGraph`, and cloud readers with explicit KEEP_LAST(1), RELIABLE, VOLATILE QoS, requires exactly one compatible volatile publisher per topic, and only then snapshots counters immediately before `call_async`. This observer uses direct single-threaded `rclpy.spin_once`, so no callback dispatch occurs between discovery and the counter boundary. A callback retained by a destroyed subscription returns before mutating topic counters or last-message state when its generation is stale. Only the stale-callback audit count changes. An acknowledged incomplete cohort is retired before retry; an outstanding response timeout fails without retry and retires its subscriptions. Success, unexpected publisher counts, zero-publisher discovery failure, and close also clean up the topic cohort while preserving clock, odometry, and TF subscriptions. Receipts record generation, exact publisher counts, the QoS premise/contract, executor model, stale callback counts, and retirement. The adversarial regression delivers late cloud, graph, and data callbacks from attempt one after attempt two begins and verifies all three are ignored; attempt two still uses only its own fresh graph/data and the exact pre-publish cached cloud. It also checks multiple-publisher rejection, response-timeout no-retry behavior, destroy failure, discovery timeout, and cleanup. PUBRETRY-F02 found that hashing only PointCloud2 data bytes omitted interpretation-critical metadata. The canonical fingerprint now binds the exact header stamp/frame, dimensions, ordered field name/offset/datatype/count records, endianness, point and row strides, density, raw length, and raw SHA-256. A focused test independently mutates every bound component. The two scoped adversarial tests passed in 4.807 seconds with approved filesystem access. No replay or full suite was run; fresh exact-source review remains required.

Exact review of `25b1cb319dbbc2a4901359766103d7e65f7e1d31` returned **REQUEST_CHANGES** with RETRY25B-F01 and RETRY25B-F02. RETRY25B-F01 found that a subscription-destruction failure while handling a PublishMap response timeout or other terminal response failure could replace the primary diagnosis. The response waiter now records and re-raises the original timeout, shutdown, future, or empty-response exception; cleanup failure is retained separately in the attempt receipt and attached as an exception note, while any undestroyed handle remains available for `close()` or another retirement pass. The adversarial timeout case forces `/mapGraph` destruction to fail, verifies the timeout remains primary, verifies both diagnoses are preserved, and then proves the retained handle can be cleaned up. RETRY25B-F02 found two missing fence checks. The PointCloud2 regression now changes raw bytes without changing payload length, proving that the raw SHA-256 participates independently in the canonical payload fingerprint. The subscription regression also presents one TRANSIENT_LOCAL publisher on every topic and proves rebinding rejects it and cleans up all created subscriptions, preserving the VOLATILE premise required by the generation fence. These cases are covered by the combined focused result below. No replay or full suite was run; fresh exact-source review remains required.

A further independent review of the same predecessor added **R3-F01** and **R3-F02** before this successor was committed. R3-F01 identified two additional exits that needed the same retirement guarantee: a synchronous `call_async()` exception and a post-acknowledgement cohort validation exception. Both now preserve the primary exception while retiring the active generation and updating its receipt. Deferred cleanup uses the same close-path helper to retry retained handles and changes a matching receipt to retired only after destruction succeeds, recording that close recovered the earlier failure. The focused regression exercises the service-call and post-ack stamp-validation failures and verifies no attempt subscription remains. R3-F02 extends the canonical PointCloud2 regression with independent field-name and field-list-cardinality mutations, in addition to the same-length raw-byte digest case and TRANSIENT_LOCAL publisher rejection required by RETRY25B-F02. The final exact two focused existing tests passed in 4.751 seconds with approved filesystem access. Python AST parsing and scoped `git diff --check` also passed. No replay or full suite was run; fresh exact-source review remains required.

The next reviewed runtime at source `141627cc024bb86a9e4646caebd5c1786959020b` completed bag playback but stopped before its first PublishMap attempt because the observer required every writer endpoint itself to be VOLATILE. The actual mapper log records `rtabmap(maps): latch = true`. The installed `grocery_sim_mapping` package share is stale relative to the repository launch source and does not translate `slam_latch` into RTAB-Map's `latch` parameter. Pinned RTAB-Map source creates `/mapData` with the default reliable/volatile depth-one profile, while latching creates `/mapGraph` and `/slam/map_cloud` as reliable/transient-local depth-one writers. ROS 2's requested/offered durability contract makes a transient-local writer compatible with an explicitly volatile reader and delivers new messages only; historical delivery requires a transient-local reader. Pinned `rmw_zenoh_cpp` behavior implements that distinction by querying retained writer cache only for transient-local readers. The observer's per-attempt readers remain explicitly KEEP_LAST(1), RELIABLE, VOLATILE, so recreation still fences out samples published before the reader matched. The endpoint gate now accepts writer durability only when it is exactly VOLATILE or TRANSIENT_LOCAL, requires exactly RELIABLE writer reliability, and rejects BEST_EFFORT, SYSTEM_DEFAULT, and UNKNOWN policies because their behavior cannot satisfy or prove the contract. Failure receipts now preserve per-topic matched publisher counts and every discovered writer's durability, reliability, history, depth, node, namespace, and topic type. The focused regression covers both allowed durabilities, rejects and diagnoses best-effort/unknown/system-default reliability and unknown/system-default durability, and retains all generation, cleanup, and exact cohort gates. The final exact observer/payload pair passed two tests in 4.745 seconds with approved filesystem access. A companion latch-routing test passed the source syntax checks but failed first on the worktree's pre-existing packaged-config divergence, before reaching its latch assertions; it is not evidence for this revision. Python AST parsing and scoped `git diff --check` passed. No playback or full suite was run; fresh exact review remains required.

## 2026-09-25 modern perception producer bindings revision

The first exact perception candidate `6d3104d0566156a5be80fb03d4b7f323f1583ec2` (tree `ad4204d460cac31c95b9c69ecb7212916cbabfa2`, base `d12cd1c6df00bc408240162f2fda0773b2eabf8f`) wired `run_rgb_tracking` to the repaired-v1 input-binding builder and emitted exact capture, SLAM, trajectory, RGB, bag, and depth-source provenance. Independent review returned **REQUEST_CHANGES** with three blockers. `PERC-6D-F01` showed that an empty LiDAR iterator still reported complete localization. `PERC-6D-F02` showed that artifact-controlled legacy flags bypassed generic validation. `PERC-6D-F03` showed that truth-subscribed upstream SLAM provenance was accepted.

The successor on advanced base `25b1cb319dbbc2a4901359766103d7e65f7e1d31` resolves all three findings. For `PERC-6D-F01`, LiDAR processing now records the sealed expected message count, raw messages read, valid decoded scans, unique scans, pose-covered scans, projected scans/points, and supported estimates; complete output requires exact raw/decoded count equality and nonzero pose, projection, and estimate support. For `PERC-6D-F02`, the generic validator accepts only manifests whose two legacy flags are explicitly false. It has no legacy early return or caller-supplied waiver; the existing historical v0 exception remains solely in the independently catalog-pinned `technical_views` branch. For `PERC-6D-F03`, binding and pose-contract validation require `ground_truth_subscribed is false` in the SLAM manifest and matching embedded/file-backed observer receipt, including bound observer bytes and size.

Exact focused checks on the advanced base plus the perception delta passed with approved filesystem access: `C:\isaacsim\python.bat -m unittest tests.test_perception tests.test_technical_views -q` ran 45 tests in 5.917 seconds; the affected complete-presentation producer-consumer test ran 1 test in 14.061 seconds; Python bytecode compilation of the four changed source/test modules passed; and scoped `git diff --check` passed. The sealed capture `runs/20260925-183307101/capture` was not modified. No replay or full suite was run. The successor requires fresh exact-candidate independent review; these programmer checks do not self-approve it.

Independent review of successor `9bbd9f0c7bec657e41893e11ba886ed345860edb` (tree `9dedda11354b70bc36ed4bf3b9c7fe509eaf99ae`) returned **REQUEST_CHANGES** with two further blockers. `PERC-9BB-F01` showed that removing both the embedded observer and observer artifact let modern SLAM bindings pass, so the repaired-v1 chain did not actually require its isolation receipt. `PERC-9BB-F02` showed that a successfully decoded but all-nonfinite scan counted toward exact valid-scan coverage before it produced any usable XYZ.

The next successor on advanced base `805cd2e929cfa7ffc4ff9b3899940c9d372efa98` resolves both findings. For `PERC-9BB-F01`, modern binding now requires the embedded observer and its artifact binding, the exact canonical `slam_observer.json` path, matching hash and size, file/embedded JSON equality, and `ground_truth_subscribed is false`. For `PERC-9BB-F02`, raw and decoded counts remain separately visible, while a new usable-finite scan count advances only after finite sampled XYZ exists; complete output requires that count to equal the sealed expected LiDAR message count. The regression combines two decoded messages with only one finite usable scan and proves that even nonzero projected/supporting output remains incomplete.

Four directed regression methods covering observer omission, truth subscription, empty LiDAR, nonfinite LiDAR, and the real producer path passed in 0.416 seconds. The exact focused perception/technical modules then passed 46 tests in 6.088 seconds, the affected complete-presentation producer-consumer test passed one test in 14.195 seconds, Python bytecode compilation passed, and scoped `git diff --check` passed. No replay or full suite was run, and the sealed capture was not modified. Fresh exact-candidate independent review remains required.

## 2026-09-25 current-vs-R7 RGB evidence packet

The self-identifying packet at `review/evidence/current-goal-20260925/` binds the preserved unlabelled Current-vs-R7 comparison assets to the exact capture manifests, RGB videos, and identical 613-row RGB timestamp indices for Current capture `20260925-183307101` at source `48de461b42d5e0945b21432cef9f5523cc5b0874` and R7 capture `20260925-041644489` at source `3dc5107e8fedee3835259282e1655749ec7438c0`. Its labeled 3840x1080 video shows the two capture/source identities plus the exact sensor timestamp on every frame; its labeled six-sample sheet records frames 0, 90, 180, 360, 510, and 600 at 0.1, 3.1, 6.1, 12.1, 17.1, and 20.1 seconds. The original producer manifests and receipts remain unchanged under their logical run paths and are bound by raw-byte SHA-256 and size in `manifest.json`; exact copies are omitted from this portable packet because they contain workstation-local absolute paths. No generative image/video system was used, so no generation receipt exists.

This evidence is limited to RGB visual review. Current uses a 75-degree horizontal FOV and R7 uses 90 degrees, so composition and apparent scale are not directly equivalent. Remaining visible limitations include procedural package shapes and labels, repeated facings, simplified materials, and flat artificial lighting. The current run's sealed SLAM identity is manifest SHA-256 `803ca96a2fcb4e8d294ed6adc6f73a366e9b2fea83d343c7d2087ce435b53ad4`, replay source `b9ade5b904c8c0cbea959650cc86620957d0de5e`, attempt `20260926T042347915Z-d146ed0c1bbd4edfb296f655a0b5fe36`; it is complete with 204 exact sequence nodes, stamp SHA-256 `5aab0707589b38fffb6fd9f79170d5723c81b175615a9e027a44435c5ccd92ad`, 203 directed Neighbor links, and ground-truth subscription false. Its perception manifest is complete with 613 RGB frames, 204 raw/valid/usable LiDAR scans, 82,097 projected points, 604 localized inventory rows, and ground-truth consumption false. RGB and perception are complete. The separate seven-view 720p LiDAR preview has independent visual and provenance review, but remains preview-only and delivery-ineligible; this checkpoint does not claim final production completion.

Exact review of successor `1be5e41d0ae4971baa2dd7ab43e0257ebf31488f` (tree `45fc1d2215377bc0b3818f55059972086aa1f962`, parent `f3bec7e7965887c31fdd8c6158bcd8dc7e6dd8a6`) accepted the earlier documentation corrections but returned **REQUEST_CHANGES** with `RGB-1BE-F01` and `RGB-1BE-F02`: claim-bearing inputs and sample timestamps were not all pinned and validated before output generation, and the raw six-sample sheet was not pinned before writes.

Successor `76362124067b548a784b5e04161c82a4fc110c74` (tree `bc8f093a68742cf268e3145380221f04b92e3169`, parent `1be5e41d0ae4971baa2dd7ab43e0257ebf31488f`) implemented fail-closed pre-write validation for all 12 declared inputs, including index, capture, configuration, SLAM, and perception bindings, and pinned the six-sample sheet before output writes. Its exact review returned **REQUEST_CHANGES** with blocker `RGB-763-F01`: after validation, the builder reopened mutable input paths, so later bytes could supply published claims or media.

Candidate `80ddc4a45e0476fdd73dad79f7bb0f1b95200107` (tree `35a89207cfab2c9fe08fd8dbf09052be7f6298dc`, parent `76362124067b548a784b5e04161c82a4fc110c74`) closed that mutable-input gap with verified snapshots, parsed receipt reuse, staged generation, final input rehashing, stable media validation, and manifest-last rollback for ordinary Python failures. Exact review still returned **REQUEST_CHANGES** with `RGB-80D-F01`, `RGB-80D-F02`, and `RGB-80D-F03`: transaction scratch containing machine-local receipt paths was addable and claimed unsupported privacy on Windows; the executing builder was not externally pinned and rechecked as a publication input; and process termination between file replacements had no restart journal or recovery protocol.

Reviewed successor `5129e23334d97aa915a3dd88eed7afeba95191b9` (tree `51935c2867b7ffb71686e78f25fee745e6d900cc`, parent `80ddc4a45e0476fdd73dad79f7bb0f1b95200107`) closes those findings. All snapshots, staging, journals, and rollback copies use the Git-ignored same-volume `runs/.rgb-evidence-transactions/tx-<32-lowercase-hex>` namespace; the directories inherit host ACLs and make no restrictive Windows ACL or privacy claim. The builder resolves one tracked blob OID, reads and binds that exact object (`db946f1267831f1bf21701e3383b86c107903d28`, file SHA-256 `8fbcee4b705d5a5f7639b66aec0faecbbed357b4a0fbc3b8de0b63aff47d50c0`), captures the executing bytes at startup, and rehashes the original builder path immediately before publication. Its allowlisted process-termination recovery writes atomic markers and a journal, backs up the old manifest first, publishes four data/document files, publishes the new manifest last, and retires verified transactions through restart-safe `gc-<32-lowercase-hex>` cleanup. On Windows this claims recovery after process termination and restart only; it makes no sudden power-loss or filesystem-cache durability claim.

Independent exact review of `5129e23` returned **APPROVE** with zero blockers. It closed the later `RGB-TX-F02` ref-movement race by resolving the blob OID once before `git cat-file`; independent checks passed ref movement before and after resolution, final builder-path rehash, all 10 publication rename failure boundaries, 22 crash/journal-lag states, nine mixed prior-file states, two pre-`PREPARED` states, idempotent commit/rollback, junction rejection, ignore coverage, and all five manifest output bindings. The labeled MP4, PNG, and sample-time bytes remain unchanged at SHA-256 `ab4071bb11d6590d5c21c5d379f7a756ed917c159c785499e0ae3ba048568e7f`, `bc1d0c517a6df0ae039a307919aa5aadd84d1ad38efc424783aa1580705b760a`, and `09584844769c1d2b6ffca54d339624c06da7772280654523a7e318c44adb82b7`; regenerated `README.md` and `manifest.json` hash to `2354da76bcb56253a3c42286915776ceed5850d5a4f0245d917f9465ee53c91e` and `a5d78331c8a0bba57e3d861eeca769a922724ee46f095fec8b83f8dcb6a3fac4`. This is a scoped RGB evidence-builder and transaction approval. External combined-candidate acceptance, owner acceptance, seven final 1080p technical clips, and the 1,350-frame final film remain pending.

## 2026-09-25 selective LiDAR technical preview

The portable packet at `review/evidence/current-goal-20260925/technical-lidar-preview/` contains a combined 27-second 1280×720 preview, its contact sheet, 21 representative frames, camera-motion trace, and a path-sanitized manifest derivative. Current capture `20260925-183307101` is bound to capture source `48de461b42d5e0945b21432cef9f5523cc5b0874`, SLAM/perception source `b9ade5b904c8c0cbea959650cc86620957d0de5e`, and catalog source `production-20260925-183307101-v1`. The preview contains 810 frames across seven ordered views and uses 204 recorded simulated PointCloud2 messages, an estimated SLAM map/trajectory, and ground-truth-free localized ROI output.

The renderer-only source delta is recorded at candidate `f3bec7e7965887c31fdd8c6158bcd8dc7e6dd8a6` (tree `0d25ec2c99b5ba93c9f320797f91521c462aa80c`, parent `961abd0b30c239697f46a6099621f24ea5588b0e`). In `simulator/technical_views.py` it changes the masthead from the unsupported `SELECTIVE REAL LIDAR` wording to `SELECTIVE RAW RETURNS` and lays out the final technical view's six provenance/footer fields across three readable lines; it does not change capture, SLAM, perception, point selection, or camera derivation. The rendered producer manifest binds `simulator/technical_views.py` at SHA-256 `c4a97911523ffb6ef40672fe455740d522d4757b9e608de5c21311e1e94221a2`. An independent focused source review of the exact `961abd0..f3bec7e` renderer diff returned **APPROVE** with zero findings. The later exact combined-candidate audit requested documentation corrections `IDXJ-F01` and `IDXJ-F03`; it did not broaden that focused source approval or certify the still-open source-review scope recorded elsewhere in this journal.

The unchanged raw producer manifest remains local at `runs/20260925-183307101/outputs/technical-views-preview-visual-fix/technical_views_preview_manifest.json`, SHA-256 `6139318c88399137a953f41b4c8d460ab62c7301bc1bb11407b98bb873dc34a3`, size 272,876 bytes. The portable derivative replaces only the three unique machine-specific absolute paths with repository-relative paths. The video SHA-256 is `ef8d27137e6c564c3c524e557b1a37c13742808278d8cc4f00a4d2f31714771d`; the camera trace SHA-256 is `4bc6adcafc8134d13afcd38a123405cac151ee9b95af511a424dca05822300c0`. Independent visual review closed the unsupported sensor wording and final-view footer clipping findings. Independent artifact review matched 23 source/producer bindings and every packaged output hash.

The clip is preview-only and `delivery_eligible:false`; it is not the seven final 1080p technical clips or 1,350-frame film. Sensor returns are simulated, projected rigidly at header timestamps without per-return timing or deskew; ROI markers show nearest-surface support only. Reviewers did not have a video playback surface, so full encoded-motion smoothness was not certified.

## 2026-09-26 — owner goal: Asimov 1 + OrcaHand grocery restocking

**Status:** ACTIVE; this is the first implementation goal after the authorized `/goal`. The required outcome remains one continuous R8 run: start in sales floor, physically walk the Asimov biped to the stock room, contact-grasp and lift a supported grocery product with the full OrcaHand, carry it back by physical gait, place/release it on physical shelf support, withdraw, and verify stability. R1–R7 are gates, not stopping points.

**Budget window:** 2026-09-26 01:18:08–13:18:08 America/Los_Angeles (08:18:08–20:18:08 UTC), 12 hours from the recorded goal creation time. The deadline does not relax acceptance.

**Authority:** The current owner goal and `C:\Users\suyog\Downloads\ROBOT_BACKROOM_RESTOCKING_IMPLEMENTATION_SPEC.md` (SHA-256 `7ea5ca5fa7558aa0a58bf94999adf545a7f6b53232fd155e2cc051cb15bcf0ad`) explicitly authorize manipulation/restocking. They supersede the older video-spec §0.3 non-manipulation scope for this separate branch; that document remains otherwise unchanged.

**Isolation/source:** Started from `origin/master` `b5eca0e4f5630390f670c6e1fbc7cfdccf05f25d` in `C:\Users\suyog\.codex\worktrees\robot-restocking\Simulation`, branch `codex/robot-restocking`. The prior approved spike commit `31a549573188688f80afc825532e376f029ce84b` (tree `282115b784912fb55556143148c779bb20659f3f`) was imported byte-for-byte as commit `078a48eb212a6ad6caedb1dfa44d13050ea8c176` (tree `9cbfb562473b9ad1acfce78d95b7ecc772a5ac9f`); the `robot_spike/` tree comparison to the approved source was empty. Upstream robot URDF/mesh files remain preserved in the spike directory; no master/G02 worktree was edited.

**Routing/resource state:** Read current startup/role instructions. Delegated reconnaissance and RST-001 are explicitly configured as `gpt-5.6-sol` / high; the worker tools do not expose observed runtime model, so that remains unknown. The orchestrator runtime identifies as GPT-6, not the project-preferred GPT-5.6 Sol/high; no supported in-session switch is exposed. Keep the separate Sol/high code-review gate. An initial `nvidia-smi --query-compute-apps` view showed PID 1872 without metadata; a later full process table identifies it as `dwm` and lists desktop C+G contexts only, with no Isaac or compute-only job visible. GPU was at P8, 7W, 47% reported utilization and 1,585 MiB / 12,282 MiB in use at 01:59 PDT. One restocking Isaac job will be scheduled at a time; check state again immediately before launch.

**Milestones:** R1 import/reset code task RST-001 is IMPLEMENTING in `C:\Users\suyog\.codex\worktrees\restock-r1-model\Simulation`, branch `work/restock-r1-model`, based on `078a48e`. Runtime import/reset remains UNVERIFIED pending a GPU slot. R2 gait, R3 reaching, R4 contact grasp/lift, R5 stationary pick/place, R6 route to stock room, R7 loaded gait, and R8 continuous restocking remain OPEN. No completion claim is made.

**Next action:** Review the exact RST-001 candidate; validate the free-base robot in Isaac when GPU is idle; then proceed to physically stable gait and manipulation milestones. Maintain `review/robot_manipulation/STATUS.md` and `ROBOT_MODEL_CHANGES.md` in the task branch.

### 2026-09-26 — restocking implementation candidates RST-001/RST-002

The isolated RST-001 production robot-model/import/reset seam is submitted at `48860236e2a07b160f1c8dd909c34febde3cc62b` (tree `a2c0437a362bcca44c457dad84b9b0f9e35991fa`), based exactly on `078a48eb212a6ad6caedb1dfa44d13050ea8c176` (tree `9cbfb562473b9ad1acfce78d95b7ecc772a5ac9f`). It adds a separate free-base production URDF/config/import/reset interface, CPU validations, tests, and model-change/status notes; the reviewed spike source and original assets are unchanged. The programmer reports 10 focused CPU tests passed; exact-candidate independent review is in progress. Isaac articulation import/reset has not been run, so R1 runtime remains open.

The independent RST-002 environment/product fixture is submitted at `bbb826f01fdfe5f8bf8ffd4bbd8c739651956daa` (tree `11a318d7cfcf3f4fa272627126edda84703243c1`), based exactly on `8f47b879802d960c93b1f48609c70958cbe36228` (tree `3e9b97cf84d7c351b0bb761a2c6d2988873b6904`). It adds a connected stockroom route/layout, physical support surfaces, and a dynamic pasta-box specification/builder under `/World/Restocking`; the programmer reports 10 focused CPU tests passed. Exact-candidate independent review is in progress. No Isaac/PhysX stage smoke or dynamic drop/stability test has run; physical fixture validity remains open.

Both candidates preserve separate immutable programmer snapshots and have dedicated Sol/high reviewers. Neither candidate has been merged. GPU jobs remain serialized; all locomotion, reach, contact grasp/lift, loaded carry, placement/withdrawal, and continuous R8 acceptance remain open.

Independent Sol/high review APPROVED the exact RST-001 code candidate `48860236e2a07b160f1c8dd909c34febde3cc62b` (tree `a2c0437a362bcca44c457dad84b9b0f9e35991fa`) on the stated base and specification, with zero findings. Reviewer independently confirmed the production URDF changes only robot name and relative mesh paths, all 93 asset hashes/sizes match the upstream manifest, name-bound commands use articulation drive targets, and the explicit reset restores root pose/velocity plus joint state/targets. The exact focused CPU suite passed 10/10 under approved access. Reviewer configured Sol/high; observed runtime identity/effort remained unknown. This is code-seam approval only: no Isaac workload ran, and the R1 runtime gate and R2–R8 remain open. The candidate now requires a trial merge against the current integration branch and fresh combined-state review.
